# Qz-Miner 5.3.1

> 版本号待发布定档：文件名 `5.3.1` 为候选编号（HEAD 当前位于 `5.3.0` 标签提交，本轮不改协议/wire，patch 语义成立）。
> 若用户定档为 `5.4.0` 等其它版本，重命名本文件即可；本文只描述改动与验证边界，不表示发布状态。

## 更新

- 依赖基线对齐 Qz-UILib：GTNH 基线升至 `2.9.0-beta-3`，真源收敛为 `dependencies.gradle` 的
  `elytraModpackVersion { setGtnhVersion("2.9.0-beta-3") }` 单值声明；`gradle.properties` 不再声明
  `elytra.manifest.*`。实测运行期组件版本：GT5 5.09.54.133、GTNHLib 0.11.46、Hodgepodge 2.7.196、
  NewHorizonsCoreMod 2.9.61、Et-Futurum-Requiem 2.6.58-GTNH、lwjgl3ify 3.0.31、Angelica 2.2.10。
- 多基线 CI 机制退役：删除 `gradle/gtnh-baselines.json`、`scripts/verify-gtnh-baselines.ps1` 与
  `build.gradle.kts` 的 `verifyGtnhBaseline` 任务；`branch-ci.yml` 收敛为单基线作业（保留 JDK 列表、
  `setupCIWorkspace`、`test`/`check`/`build`、`Tags.VERSION` 生成源与主 jar 常量池校验），
  `release-tags.yml` 删除基线解析并去掉全部 `-Pelytra.manifest.version` / `-Pqz.gtnh.*`。
- 接入 Qz-UILib 4.9.0 dev 制品（`libs/qz_uilib-4.9.0-dev.jar`），`@Mod` 依赖范围改为
  `required-after:qz_uilib@[4.9.0,5.0.0)`。
- HUD 接入迁移到 Qz-UILib 4.9 虚拟窗口契约：`ui.hud.api` 删除 `CompactHud` / `HudSnapshotProvider` /
  `HudLine` / `HudSnapshot` / `HudSpan` / `HudTone` / `TextHud`，新增 `HudWindowFactory` /
  `HudLayoutService` / `HudToolbarService` 等。Miner 侧删除 `QzMinerHudSnapshotProvider`，改为
  `QzMinerHudModel`（业务状态 → 不可变语义模型）+ `QzMinerHudWindow implements HudWindowFactory`
  （`Signal<Model>` + `SceneRuntime.forEach` keyed 列表 + `SceneChromeTokens.HUD_TEXT_*`）
  + `QzMinerHudTicker`（`ClientTickEvent.END` 冲刷）；注册仍是 `ClientProxy.init` 单点
  `ClientHudService.register(HudSpec("qz_miner:chain-status", HudAnchor.TOP_LEFT), factory)`，断线不重注册、不 close。
  - 功能等价点：内容集合与行序不变；锚点仍为左上角、窗口显隐仍由 UILib 宿主负责；空模型 → 内容根零尺寸 →
    宿主整窗隐藏（对齐旧 EMPTY 快照）；色调语义 `Tone.{MUTED,INFO,SUCCESS,WARNING}` 映射
    `SceneChromeTokens.HUD_TEXT_*`，HUD 文本不含样式编码。
  - 编译断裂清单与取舍见 `docs/反馈层/errors/ERROR-20260911-uilib-49-hud-api-removal.md`。
- 连锁状态 HUD 液态玻璃化 + 外接工具栏（消费 UILib 公开材质 / 工具栏 API）：
  - 内容外观改为「液态玻璃卡片」：公开材质 API `UiBackdrop.liquidGlass(UiGlassMaterial.DARK_THIN, 10, 0.60F)`
    + 公开表面绑定 `SceneSurfaceBinder.bind`，配方从主题 `SceneTheme.Role.PANEL` 派生（只钉死滤镜参数，
    tint / 边框 / 圆角随主题）；`HudSpec.chrome(false)` 关闭宿主外壳，避免「宿主半透明外壳 + 卡片玻璃」
    双层底色。语义模型、色调 token（`SceneChromeTokens.HUD_TEXT_*`）、空内容整窗隐藏语义不变。
  - 内容结构：内容根（`WidthSizing.SHRINK` 包裹）→ 单张卡片（键稳定常驻复用，卡内行按内容键增量更新）；
    空模型 → 卡片整体卸载 → 内容根零尺寸 → 宿主整窗隐藏（对齐旧 EMPTY 语义）。
  - 外接工具栏：`HudToolbarService.getInstance().register(HUD_ID, HudToolbarSpec.builder().build(), factory)`，
    缩放 `- / 1:1 / +` 由 UILib 公共层（`HudToolbarLayer.withScaleControls`）追加，Miner 不自绘缩放按钮、
    不读写倍率状态；注册包在 `RuntimeException` 隔离中，失败只丢工具栏、不影响 HUD 主体；注册句柄常驻，
    断线不重注册、不 close。
  - 守卫：`HudArchitectureBoundaryTest` 新增「禁止 `club.heiqi.uilib.internal.*`」「禁止自绘缩放
    （`HudScaleState` / `zoomIn` / `zoomOut` / `"1:1"` / `SceneButtonPrimitive`）」「工具栏注册单点 +
    失败隔离」断言；`QzMinerHudWindowTest` 按新结构收敛，并新增宿主 `isEmptyContent` 同源布局断言
    与卡片节点复用断言。
- UILib 定档 4.9.1（替代定档前的 4.10.0 编号）：Miner 侧下界、文档与守卫全部收敛到 4.9.1——
  `@Mod` 依赖 `[4.9.1,5.0.0)`、`MyModMetadataTest` 断言与方法名同步、`HudArchitectureBoundaryTest` 改为
  「下界 = 当前交接制品版本 + 不得残留已退役下界（4.9.0 / 4.10.0）」、`QzUiLibArtifactContractTest`
  自动跟随新文件名与新下界（M3 变异自证见验证边界）。制品 `libs/qz_uilib-4.9.1-dev.jar`。
- 聊天工具栏编辑入口 + 可编辑 HUD 目标（消费 UILib 4.9.1 公开 HUD 编辑契约）：
  - 依赖下界同步：`@Mod` 依赖由 `required-after:qz_uilib@[4.9.0,5.0.0)` 改为 `[4.9.1,5.0.0)`——
    Miner 直接引用 `HudEditTarget` / `HudEditService` / `ChatActionService`，4.9.0 制品无这些类，
    留旧下界会被 Forge 依赖检查放行、随后在类加载期 `NoClassDefFoundError`。devjar 同步为
    `libs/qz_uilib-4.9.1-dev.jar`（交付 sha256 `FFE47AC645917399CB02958DA085A864481832E2B64106C4C618F9636BFB68F4` /
    3776681 bytes，jar 内 `mcmod.info` version = `4.9.1`）。UILib 先以 4.10.0 编号交接、用户定档后重新
    以 **4.9.1** 发布（提交 `f62b71ee`）：4.9.1 相对 4.9.0 为纯增量（`javap` 公开类 1048→1056、零删除
    零改签名），远端混用面保持 `acceptableRemoteVersions [4.9.0,4.10.0)`；Miner 侧下界按定档后的实际
    需要（编辑契约自 4.9.1 起）收敛到 `4.9.1`，故本版要求 `[4.9.1,5.0.0)`。
  - 可编辑目标：`HudEditService.register(HudEditTarget.builder(HUD_ID).previewFactory(...)
    .defaultPlacement(TOP_LEFT, HUD_MARGIN_PX).toolbarSpec(与关闭态同一份规格).build())`；
    注册单点 `QzMinerHudEditEntry.install`（`ClientProxy.init` 调用），以 UILib 注册表实际内容判幂等、
    逐件 `RuntimeException` 隔离、句柄常驻（断线不重注册、不 close）。
  - **预览根可命中**：UILib 编辑宿主把拖动 handler 挂在预览内容根上，命中路由只为
    `hitTestable=true` 的节点派发指针事件。HUD 工厂内容根恒不可命中（关闭态不得拦截游戏输入），
    故预览工厂另包一层可命中的拖动命中面，内部卡片/文本仍不可命中；`QzMinerHudWindowTest` 用
    结构断言 + 布局面积断言钉住这条。
  - 预览不跟随显示门：编辑会话在聊天输入屏里连锁键必然松开（门恒关闭），新增
    `QzMinerHudModel.translateForPreview` 给出当前设置下的完整卡片内容，否则预览零尺寸无法拖动；
    `translate` 的显示门语义不变（既有门测试全绿）。预览卡片`defaultPlacement` 与 `HudSpec.margin`
    共用 `QzMinerHudWindow.HUD_MARGIN_PX`，进入编辑不跳位。
  - 聊天工具栏「编辑 HUD」：`ChatActionService.register(ChatAction.builder("qz_miner:hud_edit")`)，
    label/tooltip 走 `ClientI18n`（`hud.qz_miner.edit_action.label/tooltip`，中英齐全），
    action 只发布 `HudEditService.requestEdit(HUD_ID)`；无活动聊天屏时由 UILib 静默丢弃。
  - 守卫：新增「禁止 import 编辑数学（`HudLayoutService` / `HudLayoutResolver` / `SceneEventContext`）」
    「可编辑目标与聊天动作注册单点」「预览工厂存在且拖动面可命中」「运行期下界 = 当前交接制品版本
    （4.9.1）、不得残留已退役下界（4.9.0 / 4.10.0）」断言；新增 `QzMinerHudEditEntryTest`（6 tests）覆盖注册幂等、默认放置/规格复用、真实
    `HudEditService.Host` 端口注入的触发链路、无宿主静默丢弃、失败隔离与外部 clear 后重装。
  - 制品契约守卫（G1 加固）：新增 `QzUiLibArtifactContractTest` —— 解析 `dependencies.gradle` 实际引用的
    `libs/qz_uilib-*.jar`，读 jar 内 `mcmod.info`（modListVersion 2 → modList[0].version），用与运行期同源的
    Forge `VersionRange`（1.7.10 包名 `cpw.mods.fml.common.versioning`，即 1.8+ 的
    `net.minecraftforge.fml.common.versioning`）判定其**内部版本**满足 `@Mod` 声明的区间（当前 `[4.9.1,5.0.0)`）
    （解析器不可用或规格非法时退化为显式语义比较，不静默放行）；同时断言引用文件存在、jar 文件名版本与
    `mcmod.info` 版本一致（本轮漏检的 R1 形态）。此前只有源码字符串下界守卫，交付 jar 的内部版本从未被核对——
    旧 `qz_uilib-4.9.0-4-0.3+…-dirty` 制品才会在 822 用例全绿下漏过，直到真机加载才报缺依赖。

## 兼容性

- 网络协议不变：仍是 5.3 冻结族（17 个 packet discriminator/Side、20-path 配置 schema、wire 合同 53）。
- 依赖边界：编译与运行时 Qz-UILib 最低 **4.9.1**（`[4.9.1,5.0.0)`）——公开 HUD 编辑契约
  （`HudEditTarget` / `HudEditService` / `ChatActionService`）自 4.9.1 起提供，4.9.0 不满足；
  4.9.1 相对 4.9.0 为纯增量且 UILib 远端混用面为 `[4.9.0,4.10.0)`，故 4.9.1 与 4.9.0 可双端混用，
  但**本 Mod 的运行下界以 4.9.1 为准**；GTNH 基线为单值 `2.9.0-beta-3`。
- 同 jar 兼容：运行期兼容范围含 `2.9.0-beta-2` 与 `2.9.0-beta-3`，依据是源码对 GTNH 侧组件零静态链接
  + 双基线 `compileJava`/`compileTestJava` 实证（2026-09-11）；真机运行态未验证。
- 兼容承诺的表达方式变化：同 jar 兼容不再由 CI 矩阵分别构建证明；`2.8.4` 仍不在承诺内
  （上游 POM 缺陷结论不变，见 `ERROR-20260817`）。双基线证据改为人工流程：临时切换
  `setGtnhVersion` 编译后按原始字节恢复并校验 sha256（`-P` 覆盖通道已失效）。

## 验证边界

- 制品契约守卫已做**变异自证**（2026-09-11，共三轮，均临时改 + 还原、仓库无残留）：
  - M1（断言目标不可满足）：临时把守卫断言的区间改为 `[4.11.0,5.0.0)` → 测试失败并打印
    「libs/qz_uilib-4.10.0-dev.jar 的 jar 内部版本 4.10.0 必须满足 @Mod 声明的运行期区间 [4.11.0,5.0.0)」；
  - M2（R1 形态旧制品副本）：临时把守卫目标指向 temp 下的变异副本（文件名 `qz_uilib-4.10.0-dev.jar`、
    `mcmod.info` 版本改为 `4.9.0-4-0.3+deadbeef-dirty`）→ 测试失败并打印
    「jar 内部版本 4.9.0-4-0.3+deadbeef-dirty 必须满足 @Mod 声明的运行期区间 [4.10.0,5.0.0)」；
  - M3（定档 4.9.1 后的新制品）：把 temp 下命名为 `qz_uilib-4.9.1-dev.jar` 的 4.9.0 制品副本
    （内部 `mcmod.info` version = `4.9.0`）临时设为守卫目标 → 测试失败并打印
    「jar 内部版本 4.9.0 必须满足 @Mod 声明的运行期区间 [4.9.1,5.0.0)」，证明守卫自动跟随新文件名与新下界。
  - 交接 jar 校验：sha256 `FFE47AC645917399CB02958DA085A864481832E2B64106C4C618F9636BFB68F4` / 3776681 bytes（4.9.1）。
- HUD 编辑入口 + 可编辑目标（本轮改动）已实测：`gradlew --rerun-tasks build` exit 0（2026-09-11，
  未 clean、`build/elytra_conventions/*.json` 保留）；19 个 task 全执行，含 `compileJava` /
  `compileTestJava` / `checkstyleMain` / `checkstyleTest` / `test` / `jar` / `reobfJar` / `assemble`；
  `build/test-results/test` 149 套件 / 823 tests / 0 failures / 0 errors（含制品契约守卫
  `QzUiLibArtifactContractTest` 1；HUD 四套件 `HudArchitectureBoundaryTest` 8 +
  `QzMinerHudEditEntryTest` 6 + `QzMinerHudModelTest` 8 + `QzMinerHudWindowTest` 7 = 29）。
  产物（`build/libs`，4.9.1 下界定档后的最终构建）：`qz_miner-5.3.0-dev.jar` sha256
  `4787cca22ff09313eb0e3ce841a900b49e77460722e1086699af0882ca35f5bd`、
  `qz_miner-5.3.0-dev-dev.jar` sha256 `6362d4e1c221c2c8c0e9d04f29d01bf8abdb8116fce140e7097a48387a975dce`、
  `qz_miner-5.3.0-dev-sources.jar` sha256 `5b926a9fee8a7d9ceb65bbbf01d0a3cd0383a2c2a82c481d978b01ddaaa54134`。
- HUD 液态玻璃 + 外接工具栏（前一轮改动）已实测：`gradlew --rerun-tasks build` exit 0（2026-09-11，
  未 clean、`build/elytra_conventions/*.json` 保留）；19 个 task 全执行，含 `compileJava` /
  `compileTestJava` / `checkstyleMain` / `checkstyleTest` / `test` / `jar` / `reobfJar` / `assemble`；
  `build/test-results/test` 147 套件 / 813 tests / 0 failures / 0 errors（HUD 三套件
  `HudArchitectureBoundaryTest` 7 + `QzMinerHudWindowTest` 6 + `QzMinerHudModelTest` 7 = 20）。
  产物（`build/libs`）：`qz_miner-5.3.0-dev.jar` sha256
  `36120ad20ad7c462377c024142622bfa0c325e00e473cb76a385b4ffd25eeefb`、
  `qz_miner-5.3.0-dev-dev.jar` sha256 `d123f3ff4e1313fffbf9aa48eed10637d734bc465b113e16e0ee2df09c0e9f42`、
  `qz_miner-5.3.0-dev-sources.jar` sha256 `d2a3c2dc2003da11e909eb0809b9108bbf83ea5487957c2ad390823a445115c7`。
- HUD 迁移已实测：`compileJava` / `compileTestJava` 与 `client.*` 过滤测试 exit 0；`build/test-results/test` 26 套件 /
  159 tests / 0 failures（2026-09-11，api-migrate 执行，计数经独立读取 XML 复核；HUD 三套件 17 tests）。
- 完整 `build`（checkstyle、全量 JUnit、assemble）与双基线复测**待 verifier 验证**，尚未出结论；本文件不把未执行的构建写成通过。
- 双基线编译实证为**编译期**证据（2026-09-11，GTNH 侧零静态链接；采集时工作树尚未迁移 HUD，迁移后由 verifier 复测），不包含任何运行期行为。
- 运行态（真机 client/dedicated、HUD 显示、Angelica 双版本回放、远端版本联机）未验证，
  发布前仍需用户真机验收。
