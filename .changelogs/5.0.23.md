## 本次更新

- 修正自动工具 round 的关闭收口与规划完成发布顺序，避免关闭门残留或执行阶段过早观察到规划完成。
- 修正 GregTech `5.09.54.20` 线缆兼容 profile，并保持线缆替换的既有安全边界。
- 增加受完整库存与目标身份约束的 round-scoped 空手回退租约，省去稳定批次的逐目标网络等待，同时保留每个目标的服务端实时采掘校验。
- 以通用方块实体 token 冻结 seed 身份，避免规划期间把同方块、不同方块实体误判为同一目标。
- 增加不直接依赖 TiC 的 `HarvestTool` 窄适配，兼容旧式 `null harvestTool` 采掘语义；不放宽非 TiC、显式等级、耐久或执行期权威校验。

## 兼容性说明

- 必须安装 Qz-UILib `4.6.0` 或更高兼容版本；本次更新基于 Qz-UILib `4.6.1`。
- 支持 GTNH `2.8.4` 与 `2.9.0-beta-2`，默认出包基线为 `2.9.0-beta-2`。
- 联机时客户端与服务端必须安装相同版本的 Qz-Miner。

## 验证与已知边界

- 实机加载 `5.0.22-fix-generic-tile-identity.6+fad21dabc2` 后，TiC Smeltery metadata 2 的 round 1 同时命中 seed/目标 token，`matcher=true`、冻结规划为 `CURRENT_HAND`；`workerConfirmed=8`、`queue=4`，执行消费/成功为 `4/4`，掉落正常释放并自然进入 `FINISHED`。TiC 运行态发布阻断已解除。
- 候选范围的既有自动化验证已通过，独立 review 未发现 P0/P1；本发布说明提交另通过文档纪律、事实漂移与 diff-check 门禁。
- `Off-thread read ... serving from snapshot` 是当前 worker 只读世界经 snapshot 服务的预期诊断，不作为发布风险；这不表示运行日志完全没有 warning。
- 本文件仅准备发布说明。正式版本由后续 annotated tag `5.0.23` 注入；exact-SHA branch CI、主线合并、tag workflow、GitHub Release 与外部渠道结果仍须按实际证据分别核验。
