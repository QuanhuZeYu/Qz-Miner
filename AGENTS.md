# Qz-Miner 协作规范

回答、审查结论和交付说明优先使用中文；代码标识符、命令、路径、协议字段及行业术语保留原文。

## 项目边界

- 依赖方向固定为 `Qz-Miner -> Qz-UILib`；通用 UI 能力归 UILib，Miner 承载连锁挖矿业务、网络、掉落、世界线程与接入胶水。
- 开始改动前先读本文件。实际行为与构建状态以当前源码、配置与验证结果为准；对外契约以 `docs/开发者文档/公共接口与兼容边界.md` 为准，实现与契约不一致时应指出偏差而不是默认契约已变；`docs/反馈层/errors/` 只作排障参考，不参与现状裁决。

## 高影响安全边界

- 方块破坏、掉落实体生成和世界状态修改只在服务端主线程；并行 worker 只读世界并发布规划结果。
- 并行任务在预算安全点协作式停止；不得用常规 `Future.cancel(true)`、超时强杀或绕过 `ParallelTickExecutor.endStage(...)` 屏障制造半完成状态。
- 已收集掉落属于玩家资产：缓冲不得随会话清理丢失，生成失败或异常必须保留或回填。
- 网络入包、客户端同步、玩家生命周期清理和 GPU 资源释放必须回到对应主线程及统一生命周期入口。
- 可选模组能力不可用时安全降级；探测不得触发不必要的静态初始化，不把客户端专属签名带入 dedicated server。

## 确认边界与权限归属

- 状态机、协议、持久数据、公共兼容承诺或上述边界需要改变时，先说明明确后果并取得用户确认。
- 修改本文件、核心架构边界或发布策略前取得用户确认。
- 修改历史、删除分支、丢弃提交或改动、以及会让现有内容从最终工作目录消失的 Git 操作必须先说明影响并取得用户确认；纯增量 merge 出现冲突或无法证明完整保留时也必须停止询问。
- `runClient*`、`runServer*` 与发布仍交用户或 CI；公共 API、依赖/版本、发布策略、生产操作、密钥与认证由用户决定。

## 设计取向（继承工作站 `用户偏好-工程取向.md`，冲突时以该文件为准）

- **冲突裁决顺序**：框架正确（拒绝缝补，临时实现必须写明删除条件与时机） > 动态化 > 运行效率 > 内存占用；「最小改动」指不夹带无关改动，不等于接受临时实现。
- **动态化**：不假定静态量（分辨率/缩放/字号/主题/语言/注册表/服务端配置/资源包），静态快照必须有失效通道；缓存必须有界、可失效、可释放，并答得出「让哪一层跳过什么重算」。
- **测试从简（懒汉标准）**：默认不写。先判断这条测试是否提供别处拿不到的证据——能否替代一次实机验证、能否防住一次真实回归；两者都不满足就不写。字面快照式断言必须删除；决定保留的测试优先验证行为，仅当该结构本身就是契约时才验证结构。需真实 GL/渲染上下文的部分由实机验证覆盖，验证记录标注在被验证对象自己的注释里；生产代码不为测试让路——不加接缝、依赖注入、Mock 或测试专用分支，计算难以观测时下沉为包内纯函数。
- **优先复用既有能力**：复用本仓既有遍历/工厂/生命周期入口与 Qz-UILib 的 scene、信号、通用控件；实现细节不得绕开自有抽象。

## 工作方式与验证

- 文件读取、搜索和编辑优先使用专用工具；其他终端命令先写成 Python 脚本文件，统一放在工作站根 `temp\` 目录，在本仓工作目录下以 `python D:\Code\MC\Qz工作站\temp\<脚本> <args...>` 执行；禁止命令行内联长命令、复杂管道、多层引号或内嵌脚本，禁止命令字符串拼接与 `shell=True`。
- 代码改动通过完整 build 后才作为可交付增量自动提交（本地 commit）；失败时继续定位、修复并重跑。纯文档改动可跳过 build；未执行的测试、制品或运行态不得写成通过。
- 交付前检查相关 diff、status 与近期提交风格，只暂存任务文件并自动创建本地 commit。无冲突、保留双方完整历史与内容且不删除来源分支的本地纯增量 merge 可自动执行；push、tag 与 release 仍须用户明确要求。

## 文档与踩坑

- **文档保留判据**：设计意图、契约边界与失败语义写在最近的代码注释处；施工过程与一次性结论不进长期文档，需要跨轮恢复的写短任务笔记；独立文档只保留对外边界与有独立读者的说明（接入指南、使用说明、兼容承诺、发布流程）。
- 重要踩坑优先写在缺陷所在的源码或测试注释里（与代码同处、随改随读）；仅当结论跨模块、需要读代码之外的上下文才看得懂时，才落 `docs/反馈层/errors/`。存量记录不批量搬，下次碰到该处代码时顺手搬。
