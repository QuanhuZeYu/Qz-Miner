# 交接（项目状态权威源）

> 本文件是业务状态、阻断项和待决策事项的权威源；实时 Git 分支、HEAD 与工作区状态以 `git branch --show-current`、`git rev-parse HEAD`、`git status` 为权威。
> 协作与安全边界归 `AGENTS.md`；可选任务笔记归 `.opencode/tasks/`。只有跨任务持续有效的业务状态才回流本文件。

## 1. 当前状态 Current State

- Qz-Miner `5.2.1` dedicated server 启动 hotfix 已正式发布。提交 `86a4f609a3dd14dbce7eccbd07cbf12a18f98e77` 恢复 vanilla `respawnPlayer`/`onDisconnect` 的生产 SRG remap，并为 Forge 三参数登录与 Teleporter 跨维度 overload 显式列出 MCP/SRG selector；不修改 5.2 packet、wire、protocol 或 schema。Exact-SHA branch CI [`30413515183`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30413515183) 与 tag workflow [`30413907750`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30413907750) 均为 `completed/success`；[GitHub Release](https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.2.1) 和三项资产已核验。
- `5.2.0` 仍是不可移动历史发布事实，但生产日志确认其 dedicated server 在应用 `respawnPlayer` 生命周期 Mixin 时因 MCP 名未映射而直接退出，不能作为 dedicated server 可用版本；该缺陷由 `5.2.1` 取代。5.2 严格框选爆破由 `feature/blast-cuboid-selection@62c3f916390ced8edbed8aca6556ef9c8058e81d` 经 no-ff merge `8f08e266b7a0baef7a0be70d65d08aae2ff62c9a` 合入 `5.0重构`：新增 `AREA_CUBOID_CLEAR=14`、服务端权威双点 ACK、固定成本 AABB 预览、预算化 immutable bounds 扫描，以及 permission level 4 的 `/qzminer config list|get|set|reload`。选区 volume 复用玩家 accepted `chainMaxBlocks`；未按连锁键时左/右键分别选 point1/point2并取消原动作，按键后左键正常触发且 origin 可位于框外。当前 round 冻结 bounds，重选只影响下一 round。
- 5.2 网络合同固定 17 个 discriminator：旧 `0..14` 语义保留，`PacketCuboidSelectionRequest=15/SERVER`、`PacketCuboidSelectionSync=16/CLIENT`；严格拒绝 5.1。AutoToolSwap takeover packet/coordinator、客户端库存扫描/target rematch/preview invalidation 和 `client.autoToolTakeoverEnabled` 已删除，不提供 capability negotiation 或跨 minor fallback；服务端本地 physical ledger 与五包 round/phase control 保留。
- 跨世界生命周期修复提交 `7727546dd1a95fa4bb9c64212e09c78cf8548cb6` 统一清理旧 endpoint，并以合法 `PacketChainConfigSync` 作为客户端 server-ready 证据，每个 connection generation 恰好重放一次 mode/submode/config/object-group；依赖发布身份文档修正提交为 `258fcec5afd128b22adbdbef76bfcb91a4ac7618`。用户已实机复测跨世界场景，无异常；该定向结果不替代其余 client/dedicated、鼠标动作取消、AABB/GL、框选执行及游戏内配置命令运行态，这些范围继续为 **INCOMPLETE**。
- `5.2.0` exact-SHA branch CI [`30357369645`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30357369645) 与 tag workflow [`30357875456`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30357875456) 均为 `completed/success`。Annotated tag object 为 `0582cfebadc258dad4dd1378f377b596b0d66afb`，peeled commit 固定为 `258fcec5afd128b22adbdbef76bfcb91a4ac7618`；[GitHub Release](https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.2.0) 已公开且非 draft/prerelease，三项资产字节数与 SHA-256 已核验。Modrinth/CurseForge steps 虽 success，但没有独立远端制品证据，不能宣称两个平台已发布；完整发布账本见 `docs/反馈层/决策/release-runtime-validation.md`。
- 当前 Miner 开发/测试依赖已改为 Qz-UILib `5.0.0:dev`，保持 `transitive=false`、运行时硬依赖 `required-after:qz_uilib@[5.0.0,6.0.0)` 与不内嵌策略。目标 `5.0.0` tag、Release 和 JitPack 制品尚不存在，新坐标解析、clean consumer、编译、测试及 client/dedicated 运行态均为 **INCOMPLETE**；已验证的 `4.6.3` 制品仅是 Breaking 迁移前历史证据，不能替代新依赖。
- Qz-Miner `5.1.1` 已正式发布闭环。发布候选 `refactor/server-local-auto-tool-batch@31f82b952cc0e54f952d8da2732f78fa440bfd32`
  的 push CI [`30247607564`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30247607564) 与最终
  `5.0重构@ed5ccc79b201bc77415f64eacd9989dac905e414` 的 exact-SHA CI
  [`30248326133`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30248326133) 均为
  `completed/success`；两次 CI 的 matrix prepare、GTNH `2.8.4`/`2.9.0-beta-2`、workspace、GregTech
  baseline、tests、checks、build、generated/JAR `5.1.1-ci` 断言与聚合 `build` 均 success。Merge 为
  no-ff 双父 `15fc3b8`/`31f82b9`，tree 与 candidate 相同；完整候选独立复审为 `APPROVED`，无 P0/P1/P2。
- Annotated tag `5.1.1` object 为 `e534e84fbee9d4fa070913afb09c7f566ad2d8ba`，message 为
  `[Release]: Qz-Miner 5.1.1`，peeled commit 固定为 `ed5ccc79b201bc77415f64eacd9989dac905e414`；
  tag 未签名是身份事实，不表述为签名通过。Tag workflow
  [`30248827749`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/30248827749) 绑定该 tag 与 exact SHA，
  为 `completed/success`；job `89921913237` 的 exact-SHA gate、checkout、默认 baseline、setup、
  baseline verify、build 与 Release 均 success，Maven skipped。Modrinth/CurseForge workflow step
  虽 success，但没有独立远端制品证据，不能宣称两个外部平台发布成功。
- [`5.1.1` GitHub Release](https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.1.1) database ID 为
  `360285158`，于 `2026-07-27T08:14:00Z` 发布，标题/tag 均为 `5.1.1`，非 draft/prerelease；正文与
  tagged `.changelogs/5.1.1.md` 字节级一致。三项公开资产已从公开 URL 实际下载计算 SHA-256，且与
  API size/digest 一致：`qz_miner-5.1.1.jar` 909793 bytes /
  `c27ccc4946663bd7c12b97a84706c9ea7c8fa93a0c4620bd34470f424cd1b891`，
  `qz_miner-5.1.1-dev.jar` 903419 / `b763acd3edb5476998b34fd47ddb2ee447462e0952c29a8ca7fab1f8ac62a288`，
  `qz_miner-5.1.1-sources.jar` 502499 / `fc6cc4262d1d8de2ac6f1e251db2fcb21443d6df7603158a6155314a7f2e81b4`。
  本次未额外下载解析主/dev JAR 内部 `Tags.class`，不宣称该项实证。
- `5.1.1` 业务终态包含服务端本地批量自动工具提交 `27465374eb573ba0031fd6a34f407dca1953d49c`
  与 preview origin lease 提交 `096ff9201b8c03f3d7c304aaa88c639750387716`：ordinary 路径由服务端
  主线程独占 physical ledger、实时选取候选并在批内完成接替/恢复，预览在成功破坏 frozen origin 后以
  generation-aware lease 锁定并按终态/lifecycle 释放。它们已随 `5.1.1` 发布，不再是待发布分支状态。
  Miner clean consumer、Forge status query、mixed packaged JAR、真实 client/dedicated、连续二槽/三槽
  接替、完整 lifecycle/publication failure 与 preview A→B 游戏内回归仍为非阻断 **INCOMPLETE**，
  不能由 CI、Release 或资产证据替代。
- `5.1.0` 的 candidate/merge/tag workflow、annotated tag `df396a994e4dbda754ac38149ddeb435721d900b`
  （peeled `be740e196e5cd2c1705e2d27e6689c2a86ee8631`）、公开 Release 与三资产证据继续作为不可移动历史事实；
  其完整账本见 `docs/反馈层/决策/release-runtime-validation.md`，不由 `5.1.1` 写回改写。
- 四种范围交互已在 `fix/target-local-takeover-validation` 接通并完成宽泛右键 P1 纠偏：旧 `ChainMode.INTERACT=2`、`ChainSubMode 0..11` 与既有 trigger 常量原位不变，四模式统一声明尾部新增的仓内 `RIGHT_CLICK`，既观察 Forge BLOCK，也从 AIR 动作生效前射线解析语义目标；AIR 占位坐标不进 seed。液体 BLOCK/AIR、客户端液体预览与执行期精确门共用 common-only、include-liquids 的 Item 射线数学，其它预览继续 `objectMouseOver`。liquid 仍逐目标重验冻结 seed、同种 live source、权限与当前非空手持，但不再预判容器类型/接收能力，由 Item 自决；单目标无动作、拒绝、false 或异常继续后续队列。四模式仍预算化盒扫完整立方范围，base/crop/fertilize live 重验、对象组 `32/64/127`、packet 单 int、配置 schema 与五态状态机不变。本地协议通过目标测试 `6e751e3122754601984f5624fecb47a2`、`test` `9c8954227de148b1a4f050b4e80df3ae`、`compileJava` `e88df8b058324adc8b1d4fecf1e76d49`、`check` `9cc1782c990545e1aada034169b9426f`、`build` `f1732f3621ce416b9f769231902d8a90`；两道文档门禁通过。独立 reviewer、真实 vanilla bucket/GT 或 IC2 单元/第三方 Item/GT CropCard/EFR/保护插件、client/dedicated 与 BLOCK/AIR 双事件运行态仍为 **INCOMPLETE**。
- 方块 numeric identity 当前已统一为完整 signed `int` 合同：存在目标 block ID 为 `1..Integer.MAX_VALUE`，metadata 为 `0..Integer.MAX_VALUE`，block ID `0` 保留为空气/缺席 sentinel。5.1 曾将该身份放入自动工具 takeover wire；5.2 删除该 packet 后仅在服务端本地候选模型保留完整值域。对象组 SINGLE/SET/WILDCARD、parser、跨组 overlap 与冻结热路径使用不可变 metadata domain，保留每个输入成员的 canonical 顺序和 1024-byte wire 边界；Picker 只展示 `getSubBlocks` 实际暴露的非负高 metadata。GT 空气预算合同测试已真实进入 connected-neighbor 分支，覆盖第 1024 个空气配额失败后的重试与不桥接，空气预算生产代码未改。该前序任务的目标测试与全量 `test`、`compileJava`、`check`、两道文档门禁和 diff-check 已通过；独立 reviewer、client/dedicated、真实高 ID/metadata 与 Picker 运行态仍为 **INCOMPLETE**。
- planning 空气扫描已在 `fix/target-local-takeover-validation` 统一为 context 级固定 1024:1：六个 `BudgetedChainTraverser` 均经 `PlanningCandidateWorkBudget` 在世界读取前守 cancel/`shouldYield()`，第 1024 个空气先扣 1 再提交，失败不推进 cursor/visited/frontier/queue/confirmed/余数；空气不进 matcher/consumer/frontier，AREA 继续空间扫描，CHAIN/Logging/GT 不桥接。Flood/GT seed 世界探测已下沉到首次预算化 step，Box/Tunnel/Section 的 shell/slice 初始化状态在线性化后保存，work budget=1 可恢复推进；默认 server planning budget 文档已由 64 校正为代码权威 640。目标测试与完整本地门禁证据以活动任务结果为准；client/dedicated 大空区、真实 deadline/watchdog 与 GT 线缆替换运行态仍为 **INCOMPLETE**。
- 连锁断链与爆破宽进已在 `fix/target-local-takeover-validation` 按顶层模式分流：`CHAIN_BASE/ORE/LOGGING` 的 server/preview planner 在 PlanStarted 主线程冻结“主手 + selector 排序后的背包全部工具 + 空手”能力并集，正式 matcher 与对象组扩展共用该 evaluator；candidate=false 或 matcher=false 都不入队、不增加 confirmed、不扩展邻居。`AREA_HARVESTABLE_ALL/TUNNEL/SAME_BLOCK/ORE/SECTION_CLEAR` 不绑定工具能力，只按空间、结构与世界安全门宽进并逐目标交主线程尝试。执行期继续实时复验，工具损坏或目标无候选只跳当前目标，不改写已规划拓扑；未知 null-harvestTool 工具走稳定 Item API，效率低值或采样异常仅记为 false。实际 poll 预算、零成功推进、`tryHarvestBlock` 真实结果与 GT 线缆原子例外保持；planner、packet 数量、配置 schema 和五态转移表不由 toolswap v4 改写。该前序任务的 9 类目标测试、全量 `test`、`compileJava`、`check`、文档纪律、文档事实漂移与 diff-check 已通过；client/dedicated、真实连锁/爆破/掉落窗口与大批次运行态仍为 **INCOMPLETE**。
- agent 命令入口已统一为 Python 参数列表，Gradle v1 通过 `scripts/run-gradle-opencode.py` 适配既有协议；OpenCode 使用内置默认 `build`，任务笔记按需创建。该迁移未提供真实 Gradle 或运行态证据。
- Issue #244 的隧道方向修复已在 `fix/issue-244-tunnel-direction` 以 `d5c6244` 实施：新增每玩家 `client.tunnelDirectionSource`（默认 `look_direction`，可选 `hit_face`），命中外法线取 opposite 朝方块内部；左键 pending 按维度/坐标一次性消费并在失配、松键、模式变化和生命周期时失效。服务端 accepted radius/maxBlocks/source 原子发布后 ACK，客户端预览只读 ACK source，并把 concrete face 纳入 generation identity。配置 schema/runtime 更新为 24/23 字段；wire 保留旧前缀并固定为 C2S 8/16、S2C 12/20，旧新混连降级 LOOK。目标测试、全量 `test/check/build` 与文档门禁已通过，独立 reviewer 无 P0/P1/P2。用户在基于 `af2cd5d` 的 dirty 开发包上确认原症状已修复；日志与几何交叉覆盖默认 LOOK 的 `+X/+Z/-X/+Y`，但该证据不等同于 `d5c6244` exact-SHA 实机通过。`-Y/-Z`、HIT_FACE 六面、四象限混连和 dedicated 仍为已接受的 **INCOMPLETE** 发布边界，不因 Issue 关闭或正式发布改写为通过。
- TiC null-harvestTool 的既有实机证据仍有效：加载 `5.0.22-fix-generic-tile-identity.6+fad21dabc2` 后，Smeltery metadata 2 的 round 1 双 token 命中、`matcher=true`、冻结规划为 `CURRENT_HAND`，`workerConfirmed=8`、`queue=4`、执行消费/成功 `4/4`，掉落释放并自然进入 `FINISHED`。当时按 `HarvestTool` 父类完整名识别的窄适配已被当前通用 `Item.canHarvestBlock` 方案取代；该运行证据只覆盖 TiC 场景，不等同于未知工具矩阵已实机通过。`Off-thread read ... serving from snapshot` 是 worker 只读世界的预期 snapshot 诊断，不作为发布风险，也不表述为“完全无警告”。
- GTNH 双基线发布门已迁移为数据驱动 CI：`gradle/gtnh-baselines.json` 统一记录受支持 manifest 与预期 GregTech，`gradle.properties` 保持默认出包权威；branch CI 严格校验清单后生成两个独立 clean-runner leg，逐项执行 setup、精确依赖断言、test、check、build，并由稳定 `build` 聚合门收口。tag workflow 在任何 checkout、构建或 Release 副作用前要求同 SHA、push、成功的 `branch-ci.yml` 运行，且不再删除同名 Release 或掩盖 GitHub Release 失败；本地脚本已降为 PowerShell 7 维护者可选诊断，agent 仍不获授权。默认基线的本地 qz 协议 `test`、`compileJava`、`check`、普通 `build` 及文档纪律、事实漂移、环境所有权 SelfTest、diff-check 均通过；5.0.23 候选与主线 merge exact-SHA CI 已成功，I8 两代真实掉落、client/dedicated 运行态仍为 **INCOMPLETE**。
- 规划协作取消、空手优先级与冻结 seed 预览刷新已在 `fix/empty-hand-planning-preview` 奠定：当时引入 PlanStarted 手持/背包/空手能力快照，执行期保留实时服务端权威；仅合法空 anchor DECLINED 可空手兜底，APPLIED 重验，round=0 不进工具 round；planning cancel/complete 线性化；三种库存布局首次可见后复用原 seed 刷新预览。能力快照的当前适用范围已由本节首项收窄为 CHAIN，AREA 改为宽进；wire、协议版本、配置 schema、五态转移表和 renderer/HUD 未改。client/dedicated 与真实 HUD/预览仍为 INCOMPLETE。
- 方块 Picker 源码与测试已迁移到 Qz-UILib Breaking `HostImageSource.itemIcon(ItemStack)`；同一 screen 复用 source identity，source 创建时复制完整 ItemStack，不改变配置 canonical、Picker scene 契约或业务库存语义。`dependencies.gradle` 已改为 `5.0.0:dev`，Forge 运行依赖门已改为 `[5.0.0,6.0.0)`；目标制品尚未发布，故跨仓编译/运行迁移仍未闭环。此前 Core Profile 复测仅属于旧依赖链历史证据，第三方 renderer 与完整图标矩阵仍为 INCOMPLETE。

- 上游 Qz-UILib 曾修复 Picker scene 输入、焦点投影与旧图标链：DOWN/UP 和 MOVE 共用平台 reader 物理坐标，TextInput 在建树期声明 focused signal，Portal 首次权威聚焦即可同步显示 border/caret，并保留未聚焦 hover；旧 ItemStack backend 使用可见 GUI 深度并恢复旧 zLevel。成员解析仍与用户 query 解耦，按精确 candidate key 独立解析，unknown/无 variants 编辑保留稳定 ID 进入候选替换态。`com.github.QuanhuZeYu:Qz-UILib:4.6.3:dev` 及其复测结果现为 Breaking 迁移前历史证据，不能证明当前 `itemIcon` 源码可编译或运行；GUI Scale 2 完整交互仍为 INCOMPLETE。
- Qz-Miner `5.0.21` 已正式发布。最终 feature CI [`29575171376`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29575171376) 与 merge commit `b264aceb0d3180e80bb67ffc64b6f0dd057c1f4a` 的 CI [`29575675895`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29575675895) 均在 attempt 1 全绿；annotated tag object 为 `4e4ddae524c4fd9a68b235ca148e90f65dacd3e5`，peeled commit 固定为 `b264ace`。Tag workflow [`29576125249`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29576125249) attempt 1 的核心 setup、assemble 与 GitHub Release 成功，Maven skipped；Modrinth/CurseForge 步骤虽为 success，但配置为空且无实际外部发布证据。
- Qz-Miner `5.0.22` 已正式发布闭环：最终主线 merge commit 为 `3befb0c98e9ec362e9f967f76babb34c0e6d209a`，annotated tag object 为 `e4e46bc84aa2503b8c9dda610aa854b94c6d63dc`，peeled commit 固定为 `3befb0c`；公开 GitHub Release 非 draft/prerelease，三项 jar/dev/sources 资产身份、字节数与 SHA-256 已核验。tag 与 Release 不可移动，后续 hotfix 不属于该发布制品 SHA。真实 client/dedicated、HUD/预览、连续工具接替、空手/模组方块、GT 矿石/线缆、LootGames 与掉落运行态仍为 **INCOMPLETE**。
- Qz-Miner `5.0.23` 已正式发布闭环，核心范围为 `5.0.22` 后六个提交：自动工具 round/规划 publication 与 GT 线缆收口、round-scoped 空手回退租约及精确失效、通用方块实体 seed token、无 TiC 直接依赖的 `HarvestTool` 窄适配。最终 merge commit 为 `a64d1d7b0ef94559033a773a6059fa3b959b8b9b`；候选 CI [`29795173509`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29795173509)、merge exact-SHA CI [`29797374514`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29797374514) 与 tag workflow [`29799955205`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29799955205) 均 attempt 1 success。Annotated tag object 为 `e0037f9d1a33620b7bad3c3066cff6d100a21fb5`，peeled commit 固定为 `a64d1d7`。
- [`5.0.23` GitHub Release](https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.0.23) 已公开且非 draft/prerelease，正文与 tagged changelog 字符一致。三项资产实字节/SHA-256 分别为：主 JAR 798735 / `5769ace3955de7b134505c85583270f202f50a6653749c0eeb98c6f6878bba2e`，dev 793051 / `f86ed4911e3b458a74ad3e6576a3934095ee7dd154997e60bb718f2b891767f9`，sources 452235 / `a757777330e6a651f9a536da2b761e44a84806d3baa21dc7e9955656c9b9aeaa`。渠道终态仅 GitHub Release success；Maven、Modrinth、CurseForge 均 skipped。独立 review 无 P0/P1；Release 正文残留“后续 tag 注入”的发布前时态仅记为 P2 观察，不反向修改历史 Release/changelog/tag。
- Qz-Miner `5.0.24` 已正式发布闭环：feature 候选 `269399ddcf10fc0bebef090a879ed26ffd4b0849` 的 branch CI [`29832837769`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29832837769)、主线 merge `350aa102b7f7c498a1212c4a8bc759b3aa787d1a` 的 exact-SHA CI [`29834175117`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29834175117) 与 release-tags run [`29835307331`](https://github.com/QuanhuZeYu/Qz-Miner/actions/runs/29835307331) 均 attempt 1 success。Annotated tag object 为 `2c30a93087c65659187b3ed5090c95d2bec97bb6`，peeled commit 固定为 `350aa10`；[`5.0.24` GitHub Release](https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.0.24) 已公开且非 draft/prerelease，正文与 tagged changelog 一致。三项资产实字节/SHA-256 分别为：主 JAR 814515 / `71649b0416a3d1ebda96f4b30c417a957f15e9678f3bebee8921657d4b6d0739`，dev 808813 / `9c0507785e0256fd4be39ac15947bd464cbd585a7d09f0334cc9e23c54388b3e`，sources 458519 / `bdd86b356d37ac43dbcf79efb9d1fcfb9076f0990576b25753f2652dcaac44e6`。渠道终态仅 GitHub Release 有实际发布证据；Maven step skipped，Modrinth/CurseForge step 虽 success，但 `:publish` 无 action、project ID 为空且无远端上传证据。Issue #244 已以[唯一回复](https://github.com/QuanhuZeYu/Qz-Miner/issues/244#issuecomment-5034905749)（ID `5034905749`）说明修复与边界，随后按 `completed` 关闭，`closed_at=2026-07-21T13:54:35Z`；独立 review 无 P0/P1/P2。
- GitHub Release <https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.0.21> 已公开且非 draft/prerelease，正文与 `.changelogs/5.0.21.md` 一致。`5.0.20` 日志确认的 EndlessIDs 16-bit metadata 与 TAKEOVER 冗余 anchor echo 门两项 P1 已由 `5.0.21` 修复；`5.0.19`/`5.0.20`/`5.0.21` 的 tag 与 Release 均为不可移动发布事实。
- Qz-UILib 权威远端依赖源为 JitPack；主动发布 GTNH Maven 不是 Miner 发布前置，也不要求 Maven 凭据。GitHub tag、Release、CI、Release assets、Maven Local、`www` 或 query 旁路都不能替代 channel-aware 制品与 clean consumer 证据。
- Qz-UILib `4.6.1` 的 annotated tag、Release/workflow、JitPack Build API 身份、canonical POM/main/`dev`/`sources` 与校验和、clean 显式 `:dev` consumer 均已通过；JitPack 组合发布通道 `.module=404` 为预期，UILib 外部门禁已完成。Miner `5.0.19` 发布前的本地协议 compileJava/test/check/build 均已终态成功。
- `5.0.21` Release 三项资产已核验：`qz_miner-5.0.21.jar` 749342 bytes / SHA-256 `42d49aad1993fecc6cdf5810c1198af795039a3edd8a1aa2f9dac902f644b735`，`qz_miner-5.0.21-dev.jar` 743983 bytes / `a011788769210213ab37a6ff558964c541168a656300a99197a2057fb254812d`，`qz_miner-5.0.21-sources.jar` 430827 bytes / `36cac92ae746e2aa39d55ee640af3cc2ce52285bdbe55ed28c4843395d9fb6cd`。修复后的真实 EndlessIDs 与连续 TAKEOVER 运行态仍为 INCOMPLETE；本次用户知情授权不等于实机通过且后续版本不自动继承。后续玩家只需提交“问题描述 + 对应日志”，由维护者分析并按需最小补强，不把测试负担转交玩家。
- 并行未合分支：`chore/sync-subagent-models-with-uilib`（agent 框架对齐；其未提交 WIP 已 stash：`WIP: chore/sync-subagent-models-with-uilib agent framework (do not drop)`，切回该分支后 `git stash pop`）
- `5.0重构` 的当前发布基线为 `5.2.1@86a4f609a3dd14dbce7eccbd07cbf12a18f98e77`；annotated tag object `67811b2d8389f49b034cf2cb49cd987cf6617c42` 固定 peeled 到该提交。既有 `5.1.1`、`5.2.0` 与 `5.2.1` tag/Release 均不可移动，发布后的文档提交不属于对应发布制品 SHA。
- 开发依赖基线：GTNH 支持项与预期 GregTech 由 `gradle/gtnh-baselines.json` 集中定义，默认 manifest 由 `gradle.properties` 决定；JitPack 目标标准坐标为 Qz-UILib `5.0.0:dev`，`gtnhsettingsconvention` 为 `2.0.25`；活动仓库不再提供 Maven Local/旧 group fallback。`5.0.0` 制品尚不存在，当前 Miner 尚无实际 `:dev` 消费证据。
- 紧凑 HUD 已使用 Qz-UILib rich span 区分标签与关键业务值；连锁键滚轮已迁移到 Forge 可取消鼠标事件入口，命中组合键时不再带动快捷栏，潜行判断跟随玩家重映射键位。自动化与双 GT 基线通过；HUD 和三类滚轮场景的运行态覆盖仍为 INCOMPLETE，按玩家 Issue 与日志回流。
- 跨版本代码当前面向清单定义的两项 GTNH 基线；fortune-cap Mixin 能力门仍同时核验 descriptor、字段读取与 fortune clamp 字节码形状，新代 GT/BW natural 字段使用 `(Z)Z` 表达式修改。默认依赖图已通过 Gradle 有界协议的 `test`、`compileJava`、`check`、普通 `build`，文档纪律、事实漂移、环境所有权 SelfTest 与 diff-check 均通过。本地双基线脚本仅为维护者可选诊断且本次 agent 未获授权执行；历史旧 beta 的 `runClient21` 曾越过 mod 初始化并登录本地世界，不能替代当前默认基线运行态。矿石 capability、GT 线缆、LootGames、完整掉落、client/dedicated 运行态均为 INCOMPLETE，按玩家 Issue 与日志回流。
- 配置权威：`config/qz_miner.yaml`（Qz-UILib 4.6.0 DraftValidator；23 字段含 `client.tunnelDirectionSource`、结构化 `client.objectGroups` 与两个自动工具字段；`yamlFile`/manager/current/static 仅成功提交发布）。运行时 `Config` 发布 22 个字段。自动工具字段的 `client.*` 路径为 schema 既有名：local owner 使用服务器自己的 `autoToolSwapEnabled/autoToolPrioritySelectors` 并冻结到 session，远程 client 不上传。服务端命令仅开放显式 `general.*` scalar 白名单并经 draft/save 事务提交。
- 自动工具协议当前仍为 v4 五包：ordinary `CHAIN/AREA` 在 `peek()` 与 `poll()` 之间走 local `PROCEED/SKIP_TARGET/STOP`，不发送逐目标请求、不产生网络 WAIT；一个 batch 可多次 mutation，publication 只在玩家/tick gate 后完整发布。5.1 takeover request、客户端 target rematch 与旧服 fallback 已删除。
- 自动工具 local session 精确绑定 endpoint/world/dimension/generation/round（含 round 0）/anchor 与 owner epoch；stale token 在扫描或库存 factory 前拒绝。松键、自然完成、取消、STOP、watchdog、logout、respawn、维度切换与 server stop 都按当前 endpoint identity 经过幂等 restore/classification → publication gate → cleanup；第三布局、endpoint 不可用与连续 publication 失败保持显式残余，不扩建第三方库存搬运系统。
- 5.0.20 真实单人日志确认的自动工具两项 P1 已随 5.0.21 发布：EndlessIDs metadata 域放宽至 0..65535，TAKEOVER 删除冗余客户端 anchor echo 门并增加 action/gate 单次固定 reason；协议 v3/int/60-byte framing、候选/ledger/库存安全门和服务端主线程写权不变。自动化、终审与发布成功不能改写修复后真实 EndlessIDs 运行态仍为 INCOMPLETE 的状态，后续按玩家 Issue 与日志回流。
- `client.objectGroups.members` 管理选择器仍基于 UILib 4.6.0：外层按“管理规则”“高级自定义”顺序左对齐；受约束 portal 使用顶部搜索和当前规则/搜索结果动态分区，并约束 overlay 焦点。当前规则采用 6×42px 双行：第一行本地化名称与 54/118px 固定操作按钮，第二行完整 canonical 与 0/136px 问题 badge；中英文、normal/pending 不移动按钮盒，且仅真实按钮命中。空查询不枚举全量候选，当前规则按精确 registry identity 独立解析；有 variants 编辑进入变体页，unknown/无 variants 保留稳定 ID 进入候选替换态。可添加结果排除合法已选规则，新增成功后保留 portal、搜索词、合法窗口与搜索焦点以连续批量添加；二次确认删除、重复提示不合并、合法未知 canonical 与 malformed 高级 raw 无损语义不变。上游 `1fec5a85` 统一固定 6×42 契约的真实行高与间距后，用户确认 14 项末成员 secondary 底部裁切已得到抑制；完整交互运行态仍为 INCOMPLETE，按玩家 Issue 与日志回流。

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/blocked-by/next/优先级。收敛后即从本节移除。

<open-items>

<item id="5.2-cuboid-runtime">
  <what>严格 5.2 框选清空的真实 client/dedicated、左右键取消、服务端射线 ACK、AABB/GL、框外触发、执行中重选和配置命令热修改仍缺运行态证据</what>
  <why>JVM/Gradle 能证明纯值状态、wire、预算游标、接线与 GL 调用结构，不能替代 Forge 鼠标事件、真实渲染状态、网络时序和世界执行</why>
  <blocked-by>后续定向 client/dedicated 回归或玩家日志/Issue</blocked-by>
  <next>保持 INCOMPLETE；按实际复现核对 point ACK revision、选区边界、当前轮冻结、chainMaxBlocks 下调重裁与命令输出</next>
  <priority>5.2.1 已发布；持续运行态回流</priority>
</item>

<item id="5.1-network-runtime">
  <what>Miner 自身 clean consumer、Forge status query，以及真实 5.1 stable/prerelease/branch/dirty dev mixed、5.0/畸形拒绝和 missing-mod client/dedicated 两侧运行态仍缺证据</what>
  <why>parser/JVM、CI、Release 与发布资产不能替代独立消费、Forge 建连查询、网络 channel 和真实运行态</why>
  <blocked-by>后续独立 Miner clean consumer，以及玩家日志、Issue 或定向运行态反馈</blocked-by>
  <next>每个 5.1 patch 独立保持 INCOMPLETE；按实际日志/Issue 最小回流，不预建无实证恢复系统</next>
  <priority>非阻断 5.1.1 已发布边界 / 持续运行态回流</priority>
</item>

<item id="auto-tool-server-local-runtime">
  <what>服务端本地批量自动工具的真实 client/dedicated、大批次连续二槽/三槽接替、segment/final restore、每玩家/tick publication 0/1、完整 lifecycle 与四象限 mixed-patch 仍缺运行态证据</what>
  <why>JVM/Gradle 能证明 owner、排序、mutation image、publication gate 与接线结构，不能替代真实 Minecraft inventory/container、网络时序、HUD/预览 observer 和第三方模组槽位行为</why>
  <blocked-by>后续玩家日志、Issue 或定向 client/dedicated 回归</blocked-by>
  <next>按真实反馈核对 `[AutoToolSwapLocal]`、最终 window 0 与执行汇总；第三布局或连续 publication 失败只有出现复现并影响正常路径时再建立最小补丁任务</next>
  <priority>5.1.1 已发布；运行态 INCOMPLETE / 非理论 corner case 阻断</priority>
</item>

<item id="NORTH_STAR校准">
  <what>NORTH_STAR.md 不变量 I1-I10 校准：本次基于现有架构边界/errors/reviews 草拟，需用户复核信条与不变量表述是否准确反映项目真实约束</what>
  <why>宪章是设定值层最高准绳，表述偏差会误导后续裁决</why>
  <blocked-by>用户复核 NORTH_STAR.md §3 信条与 §5 不变量</blocked-by>
  <next>用户校准后定稿，后续按《修订纪律》变更</next>
  <priority>中（本次迁移产物待确认）</priority>
</item>

<item id="生产包LootGames回归">
  <what>若用户反馈生产包仍异常，在真实 GTNH 2.8.0 dedicated server 补做 issue #235 启动回归与 LootGames 扫雷预览回归</what>
  <why>5.0.16 崩溃修复需在原始报障环境复测</why>
  <blocked-by>用户反馈是否仍有异常</blocked-by>
  <next>按需补做</next>
  <priority>低（按需）</priority>
</item>

<item id="P0矿石液体实机回归与合并">
  <what>P0-a 矿石类名迁移（fix/ore-classname-gtnh29）+ P0-b 液体再探测（fix/interact-liquid-refollow）实机回归：GT 矿石连锁匹配>0/预览/掉落、INTERACT 液体用连续吸取容器（如 IC2 单元/GT 流体单元）吸干净、模流体 Material.isLiquid() 假设、空探测宽限 40 tick 时序</what>
  <why>两个修复都是字符串反射/逻辑改动，编译通过不证明运行时有效；模流体 Material 判定与 scheduled tick 宽限需真机验证；普通铁桶吸一个是 vanilla 限制需用连续容器区分根因</why>
  <blocked-by>用户真机跑 runClient21/runServer25 验证</blocked-by>
  <next>用户实机回归后两分支 --no-ff 合并回 5.0重构，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="network-fifo-c2s-backpressure">
  <what>普通 FIFO 上的其他 C2S（含 `PacketLootGamesMinesweeperPreviewRequest`、`PacketKeyState` 等）仍无 per-endpoint 背压；消费时需重取在线 player 并校验实例身份</what>
  <why>本次仅收口配置 C2S（`PacketChainConfigRequest`）；不能推导全网络背压已完成，可被远程放大的入包仍走无界 FIFO</why>
  <blocked-by>独立后续实现（与本分支 YAML 配置实机合并无关）</blocked-by>
  <next>按端点 latest-wins/容量预算收口，消费前重取并校验 player 实例</next>
  <priority>P1</priority>
</item>

<item id="network-lootgames-s2c-size-bound">
  <what>LootGames 相关 S2C size 缺非负/业务上限/`readableBytes` 校验</what>
  <why>与配置网络收口无关的独立信任边界债；畸形 size 可放大解析成本</why>
  <blocked-by>独立后续实现（与本分支 YAML 配置实机合并无关）</blocked-by>
  <next>补 size 非负、业务上限与 readableBytes 校验</next>
  <priority>P2</priority>
</item>

<item id="5.0.23剩余运行态回流">
  <what>5.0.23 已发布；round CLOSE、planning publication、通用方块实体 token 与 TiC 窄适配已有同轮 Smeltery 实机证据；round-scoped 空手租约的大批次吞吐/watchdog，以及 GT 5.09.54.20 线缆 profile 在 GTNH 2.8.4/2.9.0-beta-2 的真实替换仍缺直接证据</what>
  <why>自动化能证明租约、反射 profile 与调用边界，不能替代真实空手批次吞吐、两代 GT 线缆纹理、tile identity 和网络刷新</why>
  <blocked-by>后续用户运行态回归；5.0.23 标准完整发布授权已接受该剩余边界</blocked-by>
  <next>按玩家 Issue 与日志回流；若补测，则核对稳定同 key 空手批次及两项 GTNH 基线的线缆替换与连接刷新</next>
  <priority>待运行态回流 / 5.0.23 已接受发布边界</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

当前无。

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| Qz-Miner 5.2.1 dedicated server 启动 hotfix | 修复 5.2.0 生命周期 Mixin 的生产 MCP/SRG 映射缺失；exact-SHA 双 GTNH CI、annotated tag、tag workflow、公开 GitHub Release 与三资产实 hash 已闭环。修复后 dedicated 启动、登录、重生、切维度和断线 smoke test 仍为 INCOMPLETE | 5.0重构 |
| Qz-Miner 5.2.0 标准发布与跨世界生命周期回归 | 严格框选爆破、17-packet 5.2 协议、服务端本地自动工具精简与跨世界 endpoint/generation 收口已发布；用户客户端跨世界复测无异常，但生产日志确认 dedicated server 因 `respawnPlayer` 未 remap 无法启动，已由 5.2.1 取代 | feature/blast-cuboid-selection → 5.0重构 |
| Qz-Miner 5.1.1 标准发布与持续运行态边界 | 唯一 local physical ledger、服务端实时候选、批内 mutation/restore/publication gate 与 generation-aware preview origin lease 已随 no-ff merge `ed5ccc79` 发布；candidate/merge CI、annotated tag、tag workflow、公开 GitHub Release 正文及三资产实 hash 已闭环，独立 review 无 P0/P1/P2；外部平台、clean consumer、真实连续接替/lifecycle/publication failure、preview A→B 与 client/dedicated 仍为 INCOMPLETE | refactor/server-local-auto-tool-batch → 5.0重构 |
| Qz-Miner 5.1.0 标准发布与持续运行态边界 | 候选/merge/tag 三段 CI、annotated tag、公开 GitHub Release 与三项资产已闭环；仅 GitHub Release 有外部发布证据，Miner clean consumer、Forge query 与真实 mixed/missing client/dedicated 仍为 INCOMPLETE | add/version-compatibility-5.1 → 5.0重构 |
| 四种范围交互模式与宽泛右键 P1 纠偏 | BLOCK/AIR 统一进入仓内宽泛 RIGHT_CLICK；AIR 从共享射线取得 seed，液体入口/预览/执行复用 include-liquids Item 数学；液体 Item 不再按容器能力预判。四模式盒扫、执行期分类重验、旧 ordinal/framing、七位对象组与五态状态机保持；完整本地 Gradle 与两道文档门禁通过，运行态与独立 review 待回流 | fix/target-local-takeover-validation |
| 自动工具 v4 target-local 与写后恢复 | TAKEOVER/DECLINE 独立烧号 request ID；request-local 失败只跳当前目标；mutation-once、完整库存与 ActionResult exact retry、committed pending ownership、客户端 cadence/SYNC_FAILED/乱序隔离已实施，完整本地门禁通过，独立 review 与运行态 INCOMPLETE | fix/target-local-takeover-validation |
| 方块 ID/metadata 完整非负 int 域 | 自动工具 target/capability 与对象组 selector/domain/parser/snapshot/Picker 已移除 24/16-bit 和 0..15 人为上限；wire/schema/framing 不变，GT connected-neighbor 空气配额测试缺口关闭；本地完整门禁通过，运行态与独立 review 待回流 | fix/target-local-takeover-validation |
| Qz-Miner 5.0.24 正式发布与 Issue #244 关闭 | feature/merge/tag 三个 workflow attempt 1 success；公开 GitHub Release、三项资产与渠道终态已核验，Issue 以唯一回复说明修复及边界后按 completed 关闭；剩余方向、混连与 dedicated 运行态保持 INCOMPLETE | fix/issue-244-tunnel-direction → 5.0重构 |
| Qz-Miner 5.0.23 正式发布与 TiC 运行回归 | 候选/merge/tag 三个 workflow attempt 1 success；公开 GitHub Release 与三项资产已核验，Maven/Modrinth/CurseForge 均 skipped；Smeltery 双 token、冻结规划、4/4 执行、掉落释放与自然 FINISHED 已实机确认，snapshot warning 按设计为 worker 只读诊断 | fix/generic-tile-identity → 5.0重构 |
| Qz-Miner 5.0.19/5.0.20/5.0.21 正式发布 | 三版本既有 tag 固定；5.0.21 的 feature/merge/tag CI、公开 GitHub Release 与三项资产身份均已核验，修复 5.0.20 日志确认的 16-bit metadata/TAKEOVER 两项 P1，真实 EndlessIDs 运行态仍为 INCOMPLETE | fix/auto-tool-extended-metadata-takeover-freshness → 5.0重构 |
| GTNH 2.9 fortune Mixin 启动纠偏（字节码形状能力门 + GT/BW `(Z)Z` FIELD handler） | 双基线全绿；2.9 客户端完成初始化并登录世界，无新 crash report | fix/fortune-mixin-capability-gate |
| S2C 连接 identity 闭环（handler/world generation、三 S2C netHandler、init/cleanup gate、clearPending、class boundary 测试） | 自动化全绿并已提交；已随 5.0.19 发布，运行态按玩家 Issue 与日志回流 | refactor/yaml-config-uilib-4.5.2-clean |
| Qz-UILib 4.5.3-beta-1 接入 + listener `BATCH_SAVE`/`RELOAD` 回灌测试 | 已提交；当时依赖升级至 4.6.1 并随 5.0.19 发布，运行态按玩家 Issue 与日志回流 | refactor/yaml-config-uilib-4.5.2-clean |
| S2C lifecycle token + keyed START 不可重入 + latest-wins 线性化契约修正 + 拒绝诊断 CAS | 自动化全绿并已提交；已随 5.0.19 发布，运行态按玩家 Issue 与日志回流 | refactor/yaml-config-uilib-4.5.2-clean |
| 对象组 P1：CommittedSnapshot 绑定请求与 ACK、连接 pending 有界保留/清理 | compileJava、目标测试、全量 test、两道文档门禁和 diff-check 通过；已随 5.0.19 发布，运行态按玩家 Issue 与日志回流 | add/object-group-matching |
| 配置 C2S keyed 背压（`PacketChainConfigRequest`；线性化 replace/remove + 弱 identity）+ listener COW recapture + commit 顺序 + S2C 拒绝传播 + processedEpoch + stop 清理顺序 | 终审并发丢值已兑现并提交；不含普通 FIFO 其他 C2S | refactor/yaml-config-uilib-4.5.2-clean |

## 5. 长期未回填（指针）

- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段（活跃：`D-GTCABLE-ATOM`）
- 架构边界现状 → `docs/设定值层/边界.md`
- 并行 Tick 重构技术债 → `docs/诊断层/并行Tick重构计划.md`
- 系统性风险审查 → `docs/诊断层/reviews/REVIEW-20260528-代码框架设计风险排查.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。任务级工作记忆归 `.opencode/tasks/<id>.md`；其中只有对后续任务持续有效的业务事实才回流到本文件或决策/错误预防，其余过程不复制。
