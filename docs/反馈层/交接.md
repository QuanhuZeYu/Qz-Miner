# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与 `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。
> 会话级工作记忆（单任务内、易失）不进本文件，归 `.opencode/session-handoff.md`（见 `docs/控制律层/编排模式/SESSION-HANDOFF.md`）。

## 1. 当前状态 Current State

- 当前分支：`add/object-group-matching`；当前 HEAD 精确值以 `git log -1` 为准；基线为 `refactor/yaml-config-uilib-4.5.2-clean`。对象模式扩展两批已落地：Qz-UILib `4.5.3-beta-4`、modes schema/default/migration、wire v2，以及七个既有模式的模式安全冻结 runtime/confirmed preview；五采掘模式只对扩展分支执行 harvest 门，两交互模式使用各自目标门，空扩展与非支持模式保持原行为。独立对象组模式与兼容 API 已删除，旧 0-11 ordinal 不变。禁止直接合主线。
- 并行未合分支：`chore/sync-subagent-models-with-uilib`（agent 框架对齐；其未提交 WIP 已 stash：`WIP: chore/sync-subagent-models-with-uilib agent framework (do not drop)`，切回该分支后 `git stash pop`）
- `5.0重构` 基线含新连锁框架 v2 + GT 线缆替换；本地领先 origin 待 push
- 开发依赖基线：GTNH `2.9.0-beta-1`、Qz-UILib `4.5.3-beta-4:dev`（本机 Maven Local，SHA-256 `E6987C951DCCF1FA5B670CBB39C24B4423AF1CF50B6FE8F9E8DE9D0FF521981F`；正式来源仍为 GTNH releases）、`gtnhsettingsconvention` `2.0.25`。
- 配置权威：`config/qz_miner.yaml`（raw preflight + 4.5.3-beta-4 DraftValidator；20 字段含 `{id,modes,members}` 结构化 `client.objectGroups`；`yamlFile`/manager/current/static 仅成功提交发布）。对象组 C2S 使用 wire v2 long modeMask；mask 0 合法，未知位/v1/截断/尾随/上限/共享 mode selector 重叠整包拒绝。连接/保存/RELOAD、keyed latest-wins、ACK/pending 与旧权威保留机制不变。
- opencode MCP：jetbrains / context7 / exa / grep_app；改配置后需重开 opencode 会话生效

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/blocked-by/next/优先级。收敛后即从本节移除。

<open-items>

<item id="yaml-config-review-merge">
  <what>YAML 配置权威 + Qz-UILib 4.5.3-beta-4 + 对象组 modes/model/parser/wire v2 第一批已接入本分支；自动化完成后待第二批 runtime、模式枚举、预览收口及用户实机复测</what>
  <why>配置权威与 GUI 栈替换属横切改动；自动化覆盖 raw/语义事务、YAML 恢复、cfg 导入、分侧快照、keyed lane、connection identity lifecycle、接管清理、listener recapture、对象组 fixed framing、请求快照绑定和确认结果水位，不覆盖真实 ConfigUI 交互与游戏网络环境</why>
  <blocked-by>本分支提交前的全量测试/门禁与用户 runClient21/runServer25；当前 beta-4 已在 Maven Local 可解析</blocked-by>
  <next>完成自动化门禁后，用户 runClient21/runServer25 复测对象组编辑、同步确认、两玩家隔离和无匹配不启动，再决定 --no-ff 合并</next>
  <priority>高（本会话交付）</priority>
</item>

<item id="object-group-picker-followup">
  <what>对象组 Picker 编辑交互尚未实现，当前仍通过结构化配置编辑 registry+metadata 成员</what>
  <why>本批严格收口 runtime 与预览语义，不扩大到 ConfigUI 选择器交互</why>
  <blocked-by>独立 UI 设计与实机交互验证</blocked-by>
  <next>后续实现 Picker，并保持现有 schema、确认协议和冻结 runtime 不变</next>
  <priority>中（后续）</priority>
</item>

<item id="实机回归-并行Tick">
  <what>并行 Tick 预算化协作重构阶段 1-6 落地后的实机回归：大范围 CHAIN、大范围 AREA 空区、GT 线缆替换、客户端预览快速切换、预览中断线/退出世界</what>
  <why>运行态缺自动化回归，预算化遍历协议与协作式取消需真机验证不卡顿、不吞掉落、可安全中断</why>
  <blocked-by>用户真机跑 runClient21/runServer25 验证</blocked-by>
  <next>用户实机回归上述场景，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="矿石时运实机回归">
  <what>GTNH 2.9 beta 下高等级时运采掘 GT / BW / GT++ 普通矿的掉落数量实机回归</what>
  <why>adapter 迁移后的 fortune>3 拦截 mixin 需真实掉落验证生效</why>
  <blocked-by>用户真机验证</blocked-by>
  <next>真机高时运采掘普通矿，核对掉落数量突破时运上限</next>
  <priority>待用户真机</priority>
</item>

<item id="NORTH_STAR校准">
  <what>NORTH_STAR.md 不变量 I1-I10 校准：本次基于现有架构边界/errors/reviews 草拟，需用户复核信条与不变量表述是否准确反映项目真实约束</what>
  <why>宪章是设定值层最高准绳，表述偏差会误导后续裁决</why>
  <blocked-by>用户复核 NORTH_STAR.md §3 信条与 §5 不变量</blocked-by>
  <next>用户校准后定稿，后续按《修订纪律》变更</next>
  <priority>中（本次迁移产物待确认）</priority>
</item>

<item id="生产包LootGames回归">
  <what>若用户反馈生产包仍异常，在真实 GTNH 2.8.0 dedicated server 补做 issue #235 启动回归与 LootGames 扫雷预览回归</what>
  <why>5.0.16 崩溃修复需在原始报障环境复测</why>
  <blocked-by>用户反馈是否仍有异常</blocked-by>
  <next>按需补做</next>
  <priority>低（按需）</priority>
</item>

<item id="P0矿石液体实机回归与合并">
  <what>P0-a 矿石类名迁移（fix/ore-classname-gtnh29）+ P0-b 液体再探测（fix/interact-liquid-refollow）实机回归：GT 矿石连锁匹配>0/预览/掉落、INTERACT 液体用连续吸取容器（如 IC2 单元/GT 流体单元）吸干净、模流体 Material.isLiquid() 假设、空探测宽限 40 tick 时序</what>
  <why>两个修复都是字符串反射/逻辑改动，编译通过不证明运行时有效；模流体 Material 判定与 scheduled tick 宽限需真机验证；普通铁桶吸一个是 vanilla 限制需用连续容器区分根因</why>
  <blocked-by>用户真机跑 runClient21/runServer25 验证</blocked-by>
  <next>用户实机回归后两分支 --no-ff 合并回 5.0重构，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="network-fifo-c2s-backpressure">
  <what>普通 FIFO 上的其他 C2S（含 `PacketLootGamesMinesweeperPreviewRequest`、`PacketKeyState` 等）仍无 per-endpoint 背压；消费时需重取在线 player 并校验实例身份</what>
  <why>本次仅收口配置 C2S（`PacketChainConfigRequest`）；不能推导全网络背压已完成，可被远程放大的入包仍走无界 FIFO</why>
  <blocked-by>独立后续实现（与本分支 YAML 配置实机合并无关）</blocked-by>
  <next>按端点 latest-wins/容量预算收口，消费前重取并校验 player 实例</next>
  <priority>P1</priority>
</item>

<item id="network-lootgames-s2c-size-bound">
  <what>LootGames 相关 S2C size 缺非负/业务上限/`readableBytes` 校验</what>
  <why>与配置网络收口无关的独立信任边界债；畸形 size 可放大解析成本</why>
  <blocked-by>独立后续实现（与本分支 YAML 配置实机合并无关）</blocked-by>
  <next>补 size 非负、业务上限与 readableBytes 校验</next>
  <priority>P2</priority>
</item>

<item id="5.0重构push到origin">
  <what>5.0重构本地领先 origin 大量 commits（含新连锁框架 v2 完整合入 + GT 线缆替换完整修复），需用户确认时机 push 到远端</what>
  <why>本地大量历史未推远端，丢失风险；但 push 时机由用户决定（可能等其他 polish 或分支整理）</why>
  <blocked-by>用户确认 push 时机</blocked-by>
  <next>用户拍板后 git push origin 5.0重构</next>
  <priority>低（本地稳定，不急）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- NORTH_STAR.md 信条（§3）与不变量 I1-I10（§5）表述校准
- YAML 配置权威分支实机：Mod List 配置页、保存后连锁半径同步、旧 cfg 迁移、dedicated server 启动无 client UI 类
- 实机回归一批场景的结果反馈（P0 矿石液体等）

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| S2C 连接 identity 闭环（handler/world generation、三 S2C netHandler、init/cleanup gate、clearPending、class boundary 测试） | 自动化全绿并已提交；待用户实机 | refactor/yaml-config-uilib-4.5.2-clean |
| Qz-UILib 4.5.3-beta-1 接入 + listener `BATCH_SAVE`/`RELOAD` 回灌测试 | 已提交；上游发布阻断，待用户实机 | refactor/yaml-config-uilib-4.5.2-clean |
| S2C lifecycle token + keyed START 不可重入 + latest-wins 线性化契约修正 + 拒绝诊断 CAS | 自动化全绿并已提交；待用户实机 | refactor/yaml-config-uilib-4.5.2-clean |
| 对象组 P1：CommittedSnapshot 绑定请求与 ACK、连接 pending 有界保留/清理 | compileJava、目标测试、全量 test、两道文档门禁和 diff-check 通过；待用户实机 | add/object-group-matching |
| 配置 C2S keyed 背压（`PacketChainConfigRequest`；线性化 replace/remove + 弱 identity）+ listener COW recapture + commit 顺序 + S2C 拒绝传播 + processedEpoch + stop 清理顺序 | 终审并发丢值已兑现并提交；不含普通 FIFO 其他 C2S | refactor/yaml-config-uilib-4.5.2-clean |
| YAML 配置权威 + DraftValidator + epoch mailbox + 网络整包校验 + cfg/YAML 提交事务 | 自动化纠偏完成并已提交；待上游发布/实机 | refactor/yaml-config-uilib-4.5.2-clean |
| agent 框架对齐（模型 + 编排§7/librarian/MCP；含 designer） | 门禁通过；待合回；WIP stash 保留 | chore/sync-subagent-models-with-uilib |
| GT 线缆替换完整修复（原子执行模型 + D-GTCABLE-ATOM） | 实机全通过；merge → 5.0重构 | fix/cable-atomic-replace-binding |
| 新连锁框架 v2 合入主线（阶段1-8 + 看门狗 + 掉落四级降级） | 合入 5.0重构；领先 origin 待 push | refactor/chain-framework-v2 → 5.0重构 |

## 5. 长期未回填（指针）

- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段（活跃：`D-GTCABLE-ATOM`）
- 架构边界现状 → `docs/设定值层/边界.md`
- 并行 Tick 重构技术债 → `docs/诊断层/并行Tick重构计划.md`
- 系统性风险审查 → `docs/诊断层/reviews/REVIEW-20260528-代码框架设计风险排查.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归 `docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
