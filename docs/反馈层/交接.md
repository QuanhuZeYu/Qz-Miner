# 交接（单一权威状态源）

> 本文件是项目唯一权威状态源。任何状态冲突以本文件为准（消除多头发散，建立单一误差向量）。
> 控制律（完成标准/约束/审核纪律）不入本文件，归 `AGENTS.md` 与 `docs/控制律层/编排模式/SUBAGENT-ORCHESTRATION.md`。
> 会话级工作记忆（单任务内、易失）不进本文件，归 `.opencode/session-handoff.md`（见 `docs/控制律层/编排模式/SESSION-HANDOFF.md`）。

## 1. 当前状态 Current State

- 当前分支：`5.0重构`（新连锁框架 v2 已合入主线 HEAD=4e5a71c：自建事件总线 + 5 态状态机 + ARMED 触发 + 双份预览；阶段1-8 + 看门狗推进信号修正 + 掉落释放四级降级链补全 + Config 调优 + polish 全部落地，142 tests 真绿，用户实机核心场景通过；本地领先 origin 54 commits 未 push）
- 开发依赖基线：GTNH `2.9.0-beta-1`、Qz-UILib `4.2.0:dev`、`gtnhsettingsconvention` `2.0.25`
- `./gradlew.bat compileJava` 在 GTNH 2.9 升级分支验证通过；运行态仍缺自动化回归，主要依赖编译与实机掉落验证

## 2. 当前未收敛项 Open Items

> 仿 `NORTH_STAR.md`《偏离登记》范式。每项含 what/blocked-by/next/优先级。收敛后即从本节移除。

<open-items>

<item id="GT线缆替换崩溃修复实机回归">
  <what>GT 线缆连锁替换 NPE 修复（fix/cable-atomic-replace-binding, d325b71）实机回归：单根异种线缆替换不崩 + 视觉/mID 切换正确 + 连接网络图恢复（电力传导正常）+ 带 cover/上色线缆替换后 cover/颜色保留</what>
  <why>双向绑定修复 + 直写 mConnections 位掩码 + causeCableUpdate 重建网络图为字节码推断，setBaseMetaTileEntity 双向契约与能量网络无泄漏需真机验证</why>
  <blocked-by>用户真机跑 runClient21 验证</blocked-by>
  <next>用户实机左键连锁替换线缆，核对不崩 + 线缆类型/电压真的变了 + 电力传导正常 + cover/颜色保留</next>
  <priority>待用户真机（P0 崩溃修复）</priority>
</item>

<item id="实机回归-并行Tick">
  <what>并行 Tick 预算化协作重构阶段 1-6 落地后的实机回归：大范围 CHAIN、大范围 AREA 空区、GT 线缆替换、客户端预览快速切换、预览中断线/退出世界</what>
  <why>运行态缺自动化回归，预算化遍历协议与协作式取消需真机验证不卡顿、不吞掉落、可安全中断</why>
  <blocked-by>用户真机跑 runClient21/runServer25 验证</blocked-by>
  <next>用户实机回归上述场景，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="矿石时运实机回归">
  <what>GTNH 2.9 beta 下高等级时运采掘 GT / BW / GT++ 普通矿的掉落数量实机回归</what>
  <why>adapter 迁移后的 fortune>3 拦截 mixin 需真实掉落验证生效</why>
  <blocked-by>用户真机验证</blocked-by>
  <next>真机高时运采掘普通矿，核对掉落数量突破时运上限</next>
  <priority>待用户真机</priority>
</item>

<item id="NORTH_STAR校准">
  <what>NORTH_STAR.md 不变量 I1-I9 校准：本次基于现有架构边界/errors/reviews 草拟，需用户复核信条与不变量表述是否准确反映项目真实约束</what>
  <why>宪章是设定值层最高准绳，表述偏差会误导后续裁决</why>
  <blocked-by>用户复核 NORTH_STAR.md §3 信条与 §5 不变量</blocked-by>
  <next>用户校准后定稿，后续按《修订纪律》变更</next>
  <priority>中（本次迁移产物待确认）</priority>
</item>

<item id="生产包LootGames回归">
  <what>若用户反馈生产包仍异常，在真实 GTNH 2.8.0 dedicated server 补做 issue #235 启动回归与 LootGames 扫雷预览回归</what>
  <why>5.0.16 崩溃修复需在原始报障环境复测</why>
  <blocked-by>用户反馈是否仍有异常</blocked-by>
  <next>按需补做</next>
  <priority>低（按需）</priority>
</item>

<item id="P0矿石液体实机回归与合并">
  <what>P0-a 矿石类名迁移（fix/ore-classname-gtnh29）+ P0-b 液体再探测（fix/interact-liquid-refollow）实机回归：GT 矿石连锁匹配>0/预览/掉落、INTERACT 液体用连续吸取容器（如 IC2 单元/GT 流体单元）吸干净、模流体 Material.isLiquid() 假设、空探测宽限 40 tick 时序</what>
  <why>两个修复都是字符串反射/逻辑改动，编译通过不证明运行时有效；模流体 Material 判定与 scheduled tick 宽限需真机验证；普通铁桶吸一个是 vanilla 限制需用连续容器区分根因</why>
  <blocked-by>用户真机跑 runClient21/runServer25 验证</blocked-by>
  <next>用户实机回归后两分支 --no-ff 合并回 5.0重构，反馈结果</next>
  <priority>待用户真机</priority>
</item>

<item id="5.0重构push到origin">
  <what>5.0重构本地领先 origin 54 commits（含新连锁框架 v2 完整合入），需用户确认时机 push 到远端</what>
  <why>本地大量历史未推远端，丢失风险；但 push 时机由用户决定（可能等其他 polish 或分支整理）</why>
  <blocked-by>用户确认 push 时机</blocked-by>
  <next>用户拍板后 git push origin 5.0重构</next>
  <priority>低（本地稳定，不急）</priority>
</item>

</open-items>

## 3. 待拍板项 Awaiting User

- NORTH_STAR.md 信条（§3）与不变量 I1-I10（§5，I10 已写入）表述校准
- 实机回归一批场景的结果反馈（P0 矿石液体）；新连锁框架 v2 已合入 5.0重构主线（HEAD=4e5a71c），用户实机核心场景通过
- 新连锁框架 v2 完整合入 5.0重构（阶段1事件总线+阶段2状态机5态+I10入宪章+阶段3触发链路+阶段4规划接入+阶段5执行接入+阶段6客户端纯投影+阶段7看门狗+生命周期收口+阶段8删旧框架+本会话看门狗推进信号修正+掉落释放四级降级链补全+Config调优+polish），142 tests 真绿

## 4. 最近完成 Recently Done

| 单元 | 结果 | 分支 |
|---|---|---|
| GT 线缆连锁替换 NPE 修复（绕开 connect/disconnect 高级封装，单阶段原子替换：setBaseMetaTileEntity 双向绑定 + 直写 mConnections 位掩码 + causeCableUpdate 重建网络图 + try/catch 精确回滚；RECONNECT 两阶段停用） | 编译+test 12 passed+reviewer 有条件通过（P1 文档已回写）；待实机 | fix/cable-atomic-replace-binding |
| 并行 Tick 预算化协作重构 阶段 1-6 | traverser 全预算化、maxNodes 兼容清理、工作预算配置化 | 多分支 |
| 矿石时运上限突破修复（MixinExtras 拦 fortune>3，迁移 GTOreAdapter/BWOreAdapter/GTPPOreAdapter） | 编译通过，待真机 | — |
| issue #232 掉落缓冲解耦 / #234 退世界闪退 / #235 LootGames 启动崩溃 三合一修复 | 玩家级 ChainPlayerDropBuffer + Client/ServerMainThreadDispatcher + 反射安全边界 | — |
| issue #238/#240/#241 矿石匹配失效 + #239 液体只吸几个修复（GTNH 2.9 矿石重构迁移 + INTERACT 液体再探测三重保险） | 编译+reviewer通过，待实机 | fix/ore-classname-gtnh29 + fix/interact-liquid-refollow |
| 规划线程切执行态 I1 隐性偏离补登记（AbstractFloodFillPlanningStrategy:143/157 worker 线程内 setExecutionStatus + syncPlayerState） | NORTH_STAR §8 偏离登记写入，P1 清偿 | fix/ore-classname-gtnh29 |
| P1 状态机收口重构（ChainPhaseTransitions 转移表 + 越界收口 + I10 + 诊断日志 + JVM单测 + 删setExecuting） | 编译+test+reviewer通过，**实机失败作废** | refactor/chain-phase-state-machine |
| 新连锁框架 v2 阶段1-7（事件总线+状态机5态+触发链路+规划接入+执行接入dry-run+客户端纯投影+看门狗生命周期收口；38→53→75→87→109 tests 逐阶段累加；reviewer 逐阶段放行；I1-I10/I7/G2 严守；决策文档 chain-event-bus.md 阶段1-7 段权威） | 编译+test 109 passed(11类)+阶段8 前置全部完成 | refactor/chain-framework-v2 |
| 新连锁框架 v2 阶段8 块1-3 完成（块1 删旧执行驱动ChainExecutor+6 strategy类+GT线缆补LeftClickObserved T4三事件入口+I10修订+§8偏离清偿 / 块2 真实破坏桥consumeContext接管tryHarvestBlock+G1掉落窗口四接线setExecuting+ChainStateService删切换收口+stopPlayerExecution / 块3 G2夺权HUD/预览读ClientPhaseProjection+删八字段同步链PacketChainStateSync+F3-a新建PacketChainConfigSync matchedCount真值链+共用类瘦身ChainRuntimeState 5字段+stopExecution三层死代码+ChainSession 9委托+P2-2 elapsedNanos真实化+P2-1注释清理） | 编译+test 120 passed(11类)+3轮reviewer放行(块1/块2放行,块3有条件放行P2-A/B/C已清理)+I1/I5/I7/I10/G2严守+7决策(G1/D1/A1/E1/F1-3/I10三入口)落地+主agent抽检关键file:line | refactor/chain-framework-v2 |
| 起点方块掉落收集进 buffer（ChainPlayerState 一次性+同 tick 戳 seedDropCaptureArmed / ChainPlanner.onBlockBreak publish 后 arm / ChainDropCollector 守卫扩展 executing||seedArmed；守 I1/I5/I10；4 JVM 单测+collector 登记实机） | 编译+test 124 passed(12类)+--no-ff 待合并回 refactor/chain-framework-v2 | fix/seed-block-drop-capture |
| 看门狗推进信号修正（方案 B）+ Config 默认值上调（ChainWatchdog 新增订阅 PlanProgress/ExecutionAdvanced 喂狗 + worker 每分片 publish PlanProgress + consumeContext 每破坏后 publish ExecutionAdvanced + gen 双向隔离防护；Config chainRadius 8/chainMaxBlocks 1024/maxBreakPerTick 64/serverWorkBudget 640/previewRadius 16/previewTargets 1024/watchdogTimeout 50；守 I2/I4/I10） | 编译+test 137 passed+reviewer 放行+实机通过 | fix/watchdog-progress-signal |
| 掉落释放死循环修复（ChainDropCollector onWorldTick 接通信条四四级降级链：位置→重生/出生点→已记忆兜底→100 tick 失败上限告警 discard，玩家存在/缺失两分支对称；EntityItem.forceSpawn=true 绕 chunkExists；ChainPlayerState.dropReleaseConsecutiveFailures 计数 + clearRuntimeState 清零守 I7；spawn 失败 WARN 补诊断） | 编译+test 137 passed+reviewer 放行+实机通过；守 I1/I5/I7/I10 | fix/watchdog-progress-signal |
| polish 一轮（掉落释放日志降级 spawn 失败 WARN→DEBUG 异常分支保留 + 玩家存在分支补对称 WARN 摘要 + Config 3 处注释修正 parallelTickServerWorkBudgetUnits 默认值描述/chainMaxBlocks/maxBreakPerTick 风险提示 + 新建 ChainPlayerStateDropReleaseFailureTest 5 用例守 I7/I10） | 编译+test 142 passed+reviewer 放行 | polish/drop-release-and-config |
| 新连锁框架 v2 合入 5.0重构主线（--no-ff merge commit 4e5a71c；103 文件 +8038/-1518；含阶段1-8 完整架构重写+两个 BUG 修复+Config 调优+polish） | 编译+test 142 passed+门禁通过；本地领先 origin 54 commits 待 push | refactor/chain-framework-v2 → 5.0重构 |

## 5. 长期未回填（指针）

- 宪章偏离登记 → `NORTH_STAR.md`《偏离登记》段（当前为空）
- 架构边界现状 → `docs/设定值层/边界.md`
- 并行 Tick 重构技术债 → `docs/诊断层/并行Tick重构计划.md`
- 系统性风险审查 → `docs/诊断层/reviews/REVIEW-20260528-代码框架设计风险排查.md`
- 关键取舍背景 → `docs/反馈层/决策/`

## 状态层次边界（防混淆）

本文件是**项目级**状态源（跨多任务、长期）。**会话级**工作记忆（单任务内、易失）不进本文件，归 `docs/控制律层/编排模式/SESSION-HANDOFF.md` 模式下的 `.opencode/session-handoff.md`。会话 handoff 中"有持续价值的事实"任务完成后才回流到本文件或决策/错误预防，其余丢弃。
