# 当前计划

本文件记录当前阶段的下一步动作。

## 计划

- 继续按新分层维护 `docs/记忆/`、`docs/开发者文档/` 与 `docs/使用文档/`。
- 若项目结构、命令或边界继续变化，优先写回对应长期事实文档。
- 若后续产生新的 review、error 或开放化边界变化，写回对应专题目录。
- 若协作规则继续变化，同步更新 `AGENTS.md` 与 `docs/AI记忆文档.md`。
- 后续补做 GT / BW / GT++ 普通矿 `fortune > 3` 的运行态回归验证，确认修复在真实掉落中生效。
- 若用户反馈生产包仍异常，再在真实 GTNH 2.8.0 dedicated server 包内补做 issue `#235` 启动回归，并验证 LootGames 扫雷预览仍能解析炸弹坐标。
- 补做 issue `#234` 的单人退出世界回归，并顺带验证多人进出服、模式切换、扫雷预览请求不再直接跨线程触碰状态。
- 继续研究并行 Tick 任务的协作式可暂停/可恢复/可终止设计：不能通过超时强行关闭并行空间，必须保证 `ParallelTickTask.run(...)` 内部在安全中断点主动让出，并能从已保存状态继续。
- 优先审查所有 `ChainTraverser.step(...)` 和客户端预览任务的单片工作量，确认每个分片都有严格上限，重点检查空队列/空切片推进是否也会计入预算。
- 设计时保持核心不变量：`ParallelTickExecutor.endStage(...)` 只有在 worker 全部停在安全边界后才能结束并行窗口。
- 未来发布新版本时，优先复用 `.changelogs/<tag>.md` 覆盖 release 内容，而不是依赖 GitHub 自动 notes。
- 后续新 tag 发布后，检查 release 标题是否已自动归一为 tag 名，作为发布验收项之一。

## 验证

- 检查 `docs/` 下新建链接是否存在。
- 检查根目录 `README.md` 是否新增了文档入口。
- 检查 `git status` 保持可审阅。
