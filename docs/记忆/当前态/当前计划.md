# 当前计划

本文件记录当前阶段的下一步动作。

## 计划

- 继续按新分层维护 `docs/记忆/`、`docs/开发者文档/` 与 `docs/使用文档/`。
- 若项目结构、命令或边界继续变化，优先写回对应长期事实文档。
- 若后续产生新的 review、error 或开放化边界变化，写回对应专题目录。
- 若协作规则继续变化，同步更新 `AGENTS.md` 与 `docs/AI记忆文档.md`。
- 后续补做 GT / BW / GT++ 普通矿 `fortune > 3` 的运行态回归验证，确认修复在真实掉落中生效。
- 若用户反馈生产包仍异常，再在真实 GTNH 2.8.0 dedicated server 包内补做 issue `#235` 启动回归，并验证 LootGames 扫雷预览仍能解析炸弹坐标。
- 补做 issue `#234` 的单人退出世界回归，并顺带验证多人进出服、模式切换、扫雷预览请求不再直接跨线程触碰状态。
- 并行 Tick 协作式暂停/恢复/终止研究已形成决策记录：见 `docs/记忆/决策/DECISION-20260605-并行Tick协作式暂停恢复终止.md`。
- 并行 Tick 重构阶段 1/2/3/4/5/6 已完成：框架层、任务适配层、普通洪泛、GT 线缆、伐木大邻域、盒扫、隧道盒扫、区段清理预算化和旧 `maxNodes` 遍历接口清理均已落地。
- 连锁遍历稳定边界已固定为 `BudgetedChainTraverser` + `ChainTraversalSupport`；后续新增 traverser 必须实现预算化分片，不再恢复 `ChainTraverser.step(context, int maxNodes, ...)` 兼容路径。
- 下一步优先按重构计划的实机回归建议验证：大范围 `CHAIN`、大范围 `AREA` 空区、GT 线缆替换、客户端预览快速切换、预览中断线/退出世界。
- 未来发布新版本时，优先复用 `.changelogs/<tag>.md` 覆盖 release 内容，而不是依赖 GitHub 自动 notes。
- 后续新 tag 发布后，检查 release 标题是否已自动归一为 tag 名，作为发布验收项之一。

## 验证

- 检查 `docs/` 下新建链接是否存在。
- 检查根目录 `README.md` 是否新增了文档入口。
- 并行 Tick 阶段 1/2/3/4/5/6 已运行 `./gradlew.bat compileJava` 且通过；阶段 6 已通过 grep 确认源码不再存在旧 `step(context, int maxNodes, ...)` 方法和 `MAX_SCAN_PER_SLICE` 兼容预算常量。
- 检查 `git status` 保持可审阅。
