# 当前上下文

本文件记录当前阶段目标、当前状态和下一步关注点。

## 当前阶段

- 已将 Harmes-Rust 的协作框架迁移为 Qz-Miner 的文档分层结构。
- `docs/AI记忆文档.md` 已转为总导航入口。
- 项目事实开始下沉到 `docs/记忆/` 与 `docs/开发者文档/`。

## 当前状态

- 已补齐 `docs/README.md`、`docs/记忆/README.md`、`docs/开发者文档/README.md` 这一层导航。
- 现有的错误记录和审查报告已经拆分为独立索引。
- `AGENTS.md` 已同步到新的记忆分层框架。
- 已修复 `enableUnlimitedOreFortune` 对 GT / BW / GT++ 普通矿无效的问题，现改为用 `MixinExtras` 拦截 `fortune > 3` 表达式。
- 已建立 `.changelogs/<tag>.md` 作为标准 release 覆盖文件流程，并补齐 `5.0.15` 的发布正文。
- 已补充 release 后置步骤，后续 tag 发布成功后会自动将 GitHub release 标题改写为 tag 名。
- 已为客户端生命周期清理、客户端状态同步和全部 `SimpleNetworkWrapper` 服务端入包补上主线程收口，新增 `ClientMainThreadDispatcher` 与 `ServerMainThreadDispatcher`。
- 已将 `PlayerManager` 的断线清理与单人退主菜单清理切回服务端主线程执行，避免在客户端或 Netty 线程直接触碰 `ChainStateService`。
- 已修复 issue `#235` 中 Qz-Miner 5.0.16 在 GTNH 2.8.0 dedicated server 启动崩溃的问题：LootGames 扫雷兼容不再反射扫描含客户端签名的方块方法，改为安全成员解析与稳定棋盘布局解析。
- 当前 `./gradlew.bat compileJava` 已在 issue `#235` 修复分支验证通过。
- 当前 `./gradlew.bat runServer25` 已在 dev dedicated server 环境进入 `Done` 状态，未再出现 `IIconRegister` / LootGames 反射崩溃；验证前需关闭 ServerUtilities 的 `enable_player_sleeping_percentage` 以避开与 Et Futurum 的睡眠百分比 mixin 冲突。
- `5.0.17` 已发布成功，release workflow 重跑后通过并使用 `.changelogs/5.0.17.md` 作为正文；发布地址为 `https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.0.17`。
- 当前开发依赖基线已升级到 GTNH `2.9.0-beta-1`、Qz-UILib `4.2.0` 与 `gtnhsettingsconvention` `2.0.25`；非必要整合包兼容依赖改为 non-publishable runtime 配置。
- 当前 opencode 项目配置已接入 CodeGraph MCP：`.opencode/opencode.json` 通过 Windows `cmd.exe /d /s /c` 启动 `npx -y @astudioplus/codegraph-mcp --graph-only`，配置变更需重启 opencode 才生效。
- GTNH 2.9 beta 中 GT / BW / GT++ 普通矿掉落逻辑已迁移到 `gregtech.common.ores.GTOreAdapter`、`BWOreAdapter`、`GTPPOreAdapter`；本项目矿石时运 mixin 已同步迁移到这些 adapter。
- GTNH 2.9 beta 中 `BaseMetaPipeEntity.issueClientUpdate()` 已移除；GT 线缆替换刷新改用 `issueTileUpdate()` 配合原有 texture / block / cable update。
- 当前 `./gradlew.bat compileJava` 已在 `chore/update-gtnh-2-9-codegraph` 分支验证通过；Gradle 配置阶段仍会因仓库 ownership 触发 `safe.directory` 警告并退回 `NO-GIT-TAG-SET`，但不阻止编译。

## 当前关注点

- 保持文档入口不冲突，避免重复堆积。
- 验证新文档链接是否都能落到实际文件。
- 后续继续把新增稳定事实写回 `docs/记忆/长期事实/`。
- 后续若继续升级上游依赖版本，需优先复核 `GTOreAdapter` / `BWOreAdapter` / `GTPPOreAdapter` 中的 `isNatural` 和 `fortune > 3` 注入点是否仍然稳定。
- 后续发布版本时，需要确保 `.changelogs/<tag>.md` 先提交、后打 tag；若 release workflow 因外部 Maven 仓库读超时失败，可先重跑失败 job。
- 若历史 release 标题不是 tag 名，需要手动补一次 `gh release edit <tag> --title <tag>`。
- 若用户反馈生产包仍异常，再在真实 GTNH 2.8.0 dedicated server 包内补做启动与 LootGames 扫雷预览回归。
- 需要补做单人进入世界 -> 触发预览/连锁 -> 退出世界的实机回归，确认不再出现 `No context is current`。
