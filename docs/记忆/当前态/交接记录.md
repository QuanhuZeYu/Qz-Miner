# 交接记录

本文件用于会话结束、任务中断、多人接力时的快速交接。

## 最近一次交接

- 已完成：已新建工作分支 `chore/update-gtnh-2-9-codegraph` 承载 GTNH / Qz-UILib 依赖升级与 CodeGraph 配置，避免直接在 `5.0重构` 上改动。
- 已完成：开发依赖基线已升级到 GTNH `2.9.0-beta-1`、Qz-UILib `4.2.0` 与 `gtnhsettingsconvention` `2.0.25`；Qz-UILib 4.2.0 已通过 GitHub Release 与 Gradle 解析确认可用。
- 已完成：依赖声明按 Qz-UILib 参考项目方式调整，非必要整合包兼容依赖使用 non-publishable runtime 配置，避免写入发布依赖。
- 已完成：项目级 `.opencode/opencode.json` 已注册 CodeGraph MCP，使用 `cmd.exe /d /s /c` 包装 `npx -y @astudioplus/codegraph-mcp --graph-only`，避免 Windows npm shim 与重复 `--mcp` 问题。
- 已完成：GTNH 2.9 beta 将 GT / BW / GT++ 普通矿掉落逻辑迁移到 `gregtech.common.ores.*OreAdapter`，旧 `TileEntityOres#getDrops` / `BWTileEntityMetaGeneratedOre#getDrops` / `BlockBaseOre#getDrops` 注入点已改为 `GTOreAdapter`、`BWOreAdapter`、`GTPPOreAdapter`。
- 已完成：GT 线缆替换刷新已适配 2.9 API，`issueClientUpdate()` 改为 `issueTileUpdate()` 并保留 texture / block / cable update。
- 已验证：`./gradlew.bat compileJava` 在升级后通过；本机仍会因 Git dubious ownership 输出 `safe.directory` 警告和 `NO-GIT-TAG-SET`，但不影响编译成功。
- 推荐下一步：实机回归 GTNH 2.9 beta 下高等级时运采掘 GT / BW / GT++ 普通矿、GT 线缆替换，以及 runClient/runServer 运行链路。
- 已完成：issue `#233` 已确认属于真实缺陷，原因是旧实现只改了 `Random.nextInt(...)`，未覆盖上游 `fortune > 3` 截断。
- 已完成：GT / BW / GT++ 三处普通矿时运上限修复已改为 `MixinExtras @ModifyExpressionValue` 拦截表达式。
- 已完成：`mixins.qz_miner.json` 已增加 `mixinextras.minVersion = 0.5.0`。
- 已完成：`./gradlew.bat compileJava` 编译通过。
- 已完成：已新增 `.changelogs/5.0.15.md` 并将 `.changelogs/<tag>.md` 记为标准 release 覆盖流程。
- 已完成：release workflow 已补充后置步骤，后续发布后会自动将 release 标题设置为 tag 名。
- 已完成：issue `#234` 已审查，确认单人退出世界闪退是 `ClientDisconnectionFromServerEvent` 在 `Netty Client IO` 线程直接触发 `ChainPreviewMeshCache.dispose()` 所致。
- 已完成：已对 issue `#234` 做同类问题排查，确认除断线 GL 清理外，多个 `SimpleNetworkWrapper` 处理器也缺少主线程收口，其中 `PacketLootGamesMinesweeperPreviewRequest` 和 `PacketChainStateSync` 风险最明确。
- 已完成：已新增 `ClientMainThreadDispatcher` 与 `ServerMainThreadDispatcher`，并将客户端断线清理、客户端状态同步、全部服务端网络包处理器收口到正确主线程。
- 已完成：`PlayerManager` 的断线清理与单人退主菜单清理已切回服务端主线程执行，避免直接在客户端或 Netty 线程触碰 `ChainStateService`。
- 已完成：issue `#235` 已按工程级方式修复，根因是 LootGames 扫雷兼容初始化时 `Class#getMethod` 扫描 `BoardBorderBlock`，触发服务端加载客户端专属 `IIconRegister`。
- 已完成：已新增 `ReflectiveMemberSupport`，并将可选模组类探测改为不触发静态初始化且吞掉 `LinkageError`；LootGames 扫雷兼容改为按稳定棋盘布局解析 master 与炸弹坐标，不再调用问题方块类的反射方法。
- 已完成：`./gradlew.bat compileJava` 编译通过。
- 已完成：`./gradlew.bat runServer25` 在 dev dedicated server 环境进入 `Done` 状态，未再出现 `IIconRegister` / LootGames 反射崩溃；验证时通过 `run/server/serverutilities/serverutilities.cfg` 关闭 `enable_player_sleeping_percentage` 规避 ServerUtilities 与 Et Futurum 的睡眠百分比 mixin 冲突。
- 已完成：`5.0.17` tag 已推送，release workflow 初次因 `maven.minecraftforge.net` 下载 `scala-library-2.11.5.jar` 读超时失败，重跑后成功。
- 已完成：`5.0.17` GitHub Release 已创建，标题为 `5.0.17`，正文来自 `.changelogs/5.0.17.md`，地址为 `https://github.com/QuanhuZeYu/Qz-Miner/releases/tag/5.0.17`。
- 已完成：已修复连续不松连锁键切换目标时概率少一个掉落的问题；规划线程不再在空队列时直接结束会话，最终停止由 `ChainExecutor` 主线程收口。
- 已完成：掉落释放新增实体生成失败回滚，`spawnEntityInWorld(...)` 失败或抛异常时会把未生成的掉落回填玩家级缓冲。
- 已确认：并行 Tick 执行器不能采用“等待超时后关闭窗口/强制取消 worker/让主线程继续”的熔断方案；并行窗口只有在所有 worker 分片停到安全边界后才能关闭，否则 worker 可能在安全 tick 空间外继续触碰世界状态，风险高于卡顿本身。
- 已完成：并行 Tick 协作式暂停/恢复/终止研究已沉淀为 `docs/记忆/决策/DECISION-20260605-并行Tick协作式暂停恢复终止.md`。
- 已完成：用户认可工程化重构方向，详细实施计划已写入 `docs/开发者文档/并行Tick协作式调度重构计划.md`。
- 已完成：并行 Tick 重构阶段 1 已落地，新增 `ParallelTaskResult`、`ParallelTickControl`、`ParallelWorkBudget`、`ParallelTaskState`，`ParallelTickTask.run(...)` 已升级为结构化结果返回。
- 已完成：`ParallelTickExecutor.unregister(...)` 已改为请求取消并唤醒 worker，常规取消不再移除任务或调用 `Future.cancel(true)`；`Future.cancel(true)` 仅保留在执行器最终 `shutdown()` 兜底路径。
- 已完成：`runSlicesInCurrentTick(...)` 已按 `CONTINUE/YIELDED/COMPLETED/TERMINATED` 处理状态，进入新分片前会确认窗口、阶段、tick 与剩余时间仍匹配，`awaitNextWindow()` 不再因其他阶段窗口误结束任务。
- 已完成：阶段 2 已先适配服务端规划与客户端预览任务；取消、旧 session 或旧 preview generation 会返回 `TERMINATED`，consumer 写入前会检查取消和代际/会话有效性。
- 已验证：阶段 1 后和阶段 2 后均已运行 `./gradlew.bat compileJava`，两次均通过。
- 已完成：阶段 3 已新增 `TraversalStepResult`、`BudgetedChainTraverser` 与 `ChainTraversalSupport`，任务层会优先调用预算化 traverser，旧 traverser 暂走兼容路径。
- 已完成：`ChainSearchAlgorithm.step(...)` 已增加预算化状态机，`FloodFillTraverser` 持有可恢复状态；`nextFrontier -> currentFrontier` 搬运、节点 poll、候选检查、consumer 提交和邻居生成均会消耗预算。
- 已完成：`GregTechCableTraverser` 已增加可恢复状态机，当前节点、连接方向列表、邻居游标、pending 邻居与 frontier 轮转均可跨分片恢复。
- 已验证：阶段 3 已运行 `./gradlew.bat compileJava` 且通过。
- 已完成：阶段 4 已预算化 `LoggingFloodFillTraverser`，不再在构造器里一次性构造完整 offset 列表，`seed(...)` 改为初始化 origin 邻居生成游标，首次预算化分片再逐步生成。
- 已完成：伐木遍历的节点 poll、候选检查、consumer 提交、frontier 轮转、offset 推进和邻居 canTraverse 均会消耗预算，当前中心节点、offset 游标和 pending 邻居可跨分片恢复。
- 已验证：阶段 4 已运行 `./gradlew.bat compileJava` 且通过。
- 已完成：阶段 5 已逐个预算化 `BoxScanTraverser`、`TunnelBoxScanTraverser`、`SectionClearTraverser`。
- 已完成：`BoxScanTraverser` 的壳层坐标扫描、空队列/空壳层推进、候选 poll、匹配检查和 consumer 提交已接入 `ParallelTickControl.tryConsumeWork(...)`，壳层游标和已取出的候选可跨分片恢复。
- 已完成：`TunnelBoxScanTraverser` 的 3x3 切片扫描已改为可恢复游标；`seed(...)` 只初始化状态，首个切片也在预算化分片中推进，大量空切片不会在单片内无预算连续推进。
- 已完成：`SectionClearTraverser` 的固定 16x16x16 区段壳层扫描已纳入预算协议，坐标游标、空壳层推进和 consumer 提交均可跨分片安全让出。
- 已验证：阶段 5 中每改完一个 traverser 均运行 `./gradlew.bat compileJava`，三次均通过。
- 已完成：阶段 6 已清理旧 `ChainTraverser.step(context, int maxNodes, ...)` 兼容接口，删除旧 `ChainTraverser.java`，`BudgetedChainTraverser` 已成为独立遍历接口。
- 已完成：`ChainPlanningRuntime`、`ChainModeDefinition`、`ChainSubModeDefinition`、`ChainSubModeRegistry`、服务端规划和客户端预览均已直接传递 `BudgetedChainTraverser`。
- 已完成：`ChainTraversalSupport.step(...)` 不再接收 `maxNodes` 或执行旧 traverser fallback；`FloodFillTraverser`、`GregTechCableTraverser`、`LoggingFloodFillTraverser`、`BoxScanTraverser`、`TunnelBoxScanTraverser`、`SectionClearTraverser` 和 `ChainSearchAlgorithm` 中的旧 boolean 分片方法已删除。
- 已验证：阶段 6 已运行 `./gradlew.bat compileJava` 且通过；grep 确认源码不再存在旧 `step(context, int maxNodes, ...)` 方法、`MAX_SCAN_PER_SLICE` 常量或 `maxNodes` 兼容路径。
- 已完成：并行 Tick 单片工作预算已改为可配置项；服务端使用 `general.parallelTickServerWorkBudgetUnits` 默认 `64`，客户端使用 `client.parallelTickClientWorkBudgetUnits` 默认 `640`，执行器每次创建 `ParallelWorkBudget` 时读取当前配置并做最低 `1` 的保护。
- 已验证：工作预算配置化后已运行 `./gradlew.bat compileJava` 且通过。
- 推荐下一步：按实机回归建议验证大范围 `CHAIN`、大范围 `AREA` 空区、GT 线缆替换、客户端预览快速切换、预览中断线/退出世界；继续保持不引入超时强杀、`shutdownNow()` 常规取消或主线程绕过 `endStage(...)` 等待的方案。
- 推荐下一步：若后续 release workflow 在 `Setup the workspace` 因 Maven 仓库 read timeout 失败，优先重跑失败 job；若持续复现，再考虑仓库镜像或流程调整。
- 推荐下一步：若生产包仍异常，再在真实 GTNH 2.8.0 dedicated server 环境补做启动回归和 LootGames 扫雷预览回归。
- 推荐下一步：实机回归单人退出世界、多人模式切换和扫雷预览请求，确认线程收口后无新的生命周期回归。
- 推荐下一步：在真实游戏环境验证高等级时运稿采掘 GT / BW / GT++ 普通矿的掉落数量。

## 当前关注点

- 运行态仍缺少自动化回归测试，当前修复主要依赖编译与实机掉落验证。
- 掉落竞态修复仍需实机回归：不松连锁键连续挖两类方块，确认破坏数量与最终掉落数量一致。
- 并行计算卡顿研究的当前边界：生产 traverser 和运行时类型已固定为预算化遍历协议，旧 `ChainTraverser.step(context, int maxNodes, ...)` 兼容接口已清理；后续重点转向实机回归，不要做线程强杀或主线程绕过等待。
- 若后续升级依赖版本，需要优先核对 `GTOreAdapter` / `BWOreAdapter` / `GTPPOreAdapter` 中的 `isNatural` 与 `fortune > 3` 表达式是否保持不变。
