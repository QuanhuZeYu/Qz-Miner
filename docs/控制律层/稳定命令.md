# 稳定命令

本文件记录项目可长期复用的构建、检查、测试和运行命令。

## 当前状态

- 当前项目使用 Gradle Wrapper 和 Kotlin DSL 构建脚本。

## 环境前提

- 环境所有权遵循 `AGENTS.md`「控制律层·环境所有权」：本机归用户、CI 归 runner，agent 只读核验，禁止赋值、持久修复或用 Gradle home/JDK 参数绕过。
- 执行依赖环境的命令前，仅逐项读取任务所需的非敏感变量；敏感变量只查存在性且不回显。缺失或异常时停止命令，返回 `INCOMPLETE` 并询问用户。

## 默认候选

> 本节原始 Gradle Wrapper 命令仅供用户、维护者或 CI；agent 必须使用下方 OpenCode 有界协议。

- 构建检查：`.\gradlew.bat check`
- 打包：`.\gradlew.bat build`
- 测试：`.\gradlew.bat test`
- 客户端运行：`.\gradlew.bat runClient`
- 服务端运行：`.\gradlew.bat runServer`

## 当前已验证

- 编译：`./gradlew.bat compileJava`
- Java 21 客户端启动回归：`./gradlew.bat runClient21`
- 现代 Java dedicated server 启动回归：`./gradlew.bat runServer25`
- Java 21 dedicated server 备选启动回归：`./gradlew.bat runServer21`
- 维护者可选 GTNH 双基线诊断（需 PowerShell 7）：`pwsh ./scripts/verify-gtnh-baselines.ps1`
- 推送分支：`git push origin <当前分支>`
- 推送 tag：`git push origin <tag>`

## OpenCode 有界 Gradle 协议

- 唯一入口为 `qz-gradle-opencode/v1`，合同见 [`Windows-Gradle执行协议.md`](Windows-Gradle执行协议.md)。fixer 用脚本 `Start/Poll/Wait`，不得直接 wrapper；reviewer 仅按合同复验，explorer 仅诊断既有 RunId，其他 agent 禁止。
- 普通编译 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('compileJava')`
- 普通测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test')`
- 普通检查 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('check')`
- 目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test', '--tests', '<类名或类名.方法>')`
- 5.2 兼容与 wire/schema 冻结目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test','--tests','club.heiqi.qz_miner.QzMinerNetworkVersionPolicyTest','--tests','club.heiqi.qz_miner.network.NetworkDiscriminatorRegressionTest','--tests','club.heiqi.qz_miner.network.QzMiner52WireContractTest','--tests','club.heiqi.qz_miner.config.QzMinerConfigSchemaTest','--tests','club.heiqi.qz_miner.network.AutoToolSwapNetworkRegistrationStructureTest','--tests','club.heiqi.qz_miner.network.PacketAutoToolSwapProtocolTest','--tests','club.heiqi.qz_miner.toolswap.protocol.AutoToolSwapProtocolTest','--tests','club.heiqi.qz_miner.network.PacketChainConfigProtocolTest','--tests','club.heiqi.qz_miner.network.ObjectGroupWireConfigTest','--tests','club.heiqi.qz_miner.network.PacketObjectGroupConfigSyncTest','--tests','club.heiqi.qz_miner.network.PacketChainSubModeSwitchProtocolTest','--tests','club.heiqi.qz_miner.chain.mode.ChainSubModeOrdinalRegressionTest','--tests','club.heiqi.qz_miner.objectgroup.ObjectGroupModeTest')`
- 查询：`& .\scripts\run-gradle-opencode.ps1 -Action Poll -RunId '<RunId>'`
- 等待：`& .\scripts\run-gradle-opencode.ps1 -Action Wait -RunId '<RunId>' -WaitSeconds 30`
- 范围交互 seed、计数与库存后置合同（含“不得设置 `isChangingQuantityOnly`、必须走正常 container listener”）目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test','--tests','club.heiqi.qz_miner.chain.executor.BlockInteractActionExecutorStructureTest','--tests','club.heiqi.qz_miner.chain.statemachine.ChainStateMachineTest','--tests','club.heiqi.qz_miner.chain.planner.ChainPlanningConfirmedCountContractTest','--tests','club.heiqi.qz_miner.chain.planner.ChainInteractPlannerSeedFreezeStructureTest')`
- 范围交互 seed 执行传播、作物三态与同种可排液 source 内部能力目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test','--tests','club.heiqi.qz_miner.chain.state.ChainRequestInteractionSeedTest','--tests','club.heiqi.qz_miner.chain.planner.ChainPlanningInteractionSeedPropagationStructureTest','--tests','club.heiqi.qz_miner.compat.adapter.CropGrowthStateTest','--tests','club.heiqi.qz_miner.compat.adapter.Ic2CropCompatAdapterTest','--tests','club.heiqi.qz_miner.compat.adapter.EtFuturumCropCompatAdapterTest','--tests','club.heiqi.qz_miner.chain.planner.ChainLiquidRulesTest','--tests','club.heiqi.qz_miner.chain.planner.InteractionMatcherStructureTest','--tests','club.heiqi.qz_miner.chain.planner.ChainPlanningEventBridgeTest','--tests','club.heiqi.qz_miner.chain.planner.ChainInteractPlannerSeedFreezeStructureTest')`
- 目标化液体交互执行器、共享射线、库存后置与虚拟姿态目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test','--tests','club.heiqi.qz_miner.chain.interaction.InteractionRayTraceStructureTest','--tests','club.heiqi.qz_miner.chain.executor.BlockInteractActionExecutorStructureTest','--tests','club.heiqi.qz_miner.chain.executor.InteractionInventorySupportStructureTest','--tests','club.heiqi.qz_miner.chain.executor.TargetInteractionPoseTest','--tests','club.heiqi.qz_miner.chain.executor.ServerPlayerPoseTransactionStructureTest','--tests','club.heiqi.qz_miner.chain.executor.LiquidSourceInteractActionExecutorStructureTest','--tests','club.heiqi.qz_miner.chain.planner.ChainLiquidRulesTest','--tests','club.heiqi.qz_miner.chain.state.ChainRequestInteractionSeedTest')`
- 宽泛右键 P1 纠偏目标测试 Start：`& .\scripts\run-gradle-opencode.ps1 -Action Start -GradleArgs @('test','--tests','club.heiqi.qz_miner.chain.mode.ChainInteractSubModeWiringTest','--tests','club.heiqi.qz_miner.chain.planner.ChainInteractPlannerSeedFreezeStructureTest','--tests','club.heiqi.qz_miner.chain.interaction.InteractionRayTraceStructureTest','--tests','club.heiqi.qz_miner.chain.client.ChainPreviewControllerTest','--tests','club.heiqi.qz_miner.chain.executor.LiquidSourceInteractActionExecutorStructureTest','--tests','club.heiqi.qz_miner.chain.statemachine.ChainStateMachineTest','--tests','club.heiqi.qz_miner.chain.planner.ChainPlanningInteractionSeedPropagationStructureTest','--tests','club.heiqi.qz_miner.chain.mode.ChainSubModeOrdinalRegressionTest','--tests','club.heiqi.qz_miner.network.PacketChainSubModeSwitchProtocolTest','--tests','club.heiqi.qz_miner.chain.planner.ChainLiquidRulesTest','--tests','club.heiqi.qz_miner.chain.planner.InteractionMatcherStructureTest','--tests','club.heiqi.qz_miner.chain.executor.TargetRevalidatingBlockInteractActionExecutorStructureTest')`
- 多项 Gradle 参数必须在当前 PowerShell 进程中用调用运算符 `&` 传数组；不得使用外层 `pwsh -File ... -GradleArgs @(...)`，因为该方式不能可靠传递该数组。
- 自测（不调用 Gradle）：`& .\scripts\run-gradle-opencode.ps1 -SelfTest`。
- `runClient*`/`runServer*` 交用户执行；`verify-gtnh-baselines.ps1` 暂不授权给 agent。
- 每个 5.2 patch 候选的 branch CI 使用与拟发布 tag patch 一致的 runner-owned
  `VERSION=X.Y.Z-ci+${{ github.sha }}`，并在 generated source 与主 JAR 中精确验证；当前开发 CI
  使用 `VERSION=5.2.0-ci+${{ github.sha }}`。本地 agent 不设置该变量；无注入 build 固定使用
  `5.2.0-dev`，只作为当前 5.2 family 的本地联调包，不能作为具体 patch 的发布候选 artifact 证据。

## 双基线验证

- `GRADLE_USER_HOME` 由用户在 agent 外部预先配置为纯 ASCII 路径；agent 仅只读核验。
- `gradle/gtnh-baselines.json` 是 GTNH manifest 与预期 GregTech 版本的唯一机器清单；默认开发与最终唯一出包基线只读取 `gradle.properties`，清单不复制 default、任务或缓存策略。
- 发布权威是 branch CI 从清单生成的独立 clean-runner matrix，以及 tag workflow 对同 SHA、`push`、成功 `branch-ci.yml` 运行的前置门；每个 matrix leg 固定执行 workspace setup、`verifyGtnhBaseline` 精确解析断言、test、check、build。
- `verify-gtnh-baselines.ps1` 仅为维护者可选诊断入口，要求 PowerShell 7，从脚本位置推导仓根并读取同一清单，以 no-daemon、rerun-tasks 串行执行语义断言和本地验证。该脚本不是发布门禁，workflow 不调用，agent 仍不获授权执行。
- I8 的字节码形状门与真实掉落是不同证据；client/dedicated、两代真实矿石掉落与其他运行态未完成时继续标记 **INCOMPLETE**，不得由编译或 CI 成功替代。

## 服务端验证注意事项

- 普通 `./gradlew.bat runServer` 不适合验证 Java 17-25 / `lwjgl3ify` 服务端链路；当前会走到不支持 server launch 的 relauncher 路径。
- `runServer25` / `runServer21` 使用 modern Java、`lwjgl3ify` 与 Hodgepodge，是当前服务端启动回归优先入口。
- 若 dev 环境同时启用 ServerUtilities 与 Et Futurum，需要在 `run/server/serverutilities/serverutilities.cfg` 中关闭 `B:enable_player_sleeping_percentage=false`，否则会在 Qz-Miner 初始化前触发两者睡眠百分比 mixin 冲突。

## 实机运行日志位置（真机回归传感读数来源）

> 真机回归跑完后，主 agent 直接对照下列日志确认行为，**不需用户主动贴**。日志文件大或不确定 batch 何时跑完时，派 `@explorer` 批量检索。

### `runClient21` 客户端日志（Windows 路径，相对项目根）

主目录：`run/client/logs/`

| 文件 | 用途 | 说明 |
|---|---|---|
| `fml-client-latest.log` | **主诊断入口**（含所有 `[qz_miner/]` 日志：`[ChainStateMachine] transition`/`[ChainPhaseSnapshot]`/`[ParallelTick]`/`[ChainDropCollector]`/`[ChainPreview]`/`[Network]`/`[Compat][Ore]`/`[PlayerManager]`/`[EventSystem]` 等） | 最新一次客户端启动的完整 FML 日志，启动时滚动覆盖到 `fml-client-1.log` 等 |
| `fml-client-1.log`/`-2`/`-3` | 历史滚动备份 | 越早编号越旧，用于回看上一次实机 |
| `latest.log` | minecraft 主日志子集 | 体量小，含 `Reloading ResourceManager` 等基本信息 |
| `GregTech.log` | GregTech 独立日志 | 含矿石生成/扫描等，矿石连锁调试补充 |
| `LootGames.log`/`Explosion.log` | 其他 mod 专有日志 | 对应 mod 触发时才有内容 |
| `fml-junk-earlystartup.log` | 早期启动阶段 | 启动早期 FML 自身诊断 |

**关键 qz_miner 日志特征串**（rg/grep 抓诊断）：
- 状态机转移：`[ChainStateMachine] transition <from> -> <to> on <Event> gen=N player=<UUID>`
- PlanCancelled 根因（commit b3a1c68 起带 reason）：`[ChainStateMachine] PlanCancelled reason=<reason> gen=N player=<UUID>`
- 玩家登录/登出：`[PlayerManager] Player logged in: <name>` / `[PlayerManager] Exiting to main menu, clearing N player(s)`
- 模式切换：`[KeyListener] Switched (sub|chain) mode to <X>`
- 预览任务：`[ParallelTick] Registered CLIENT_PRE task: chain-preview-<x>-<y>-<z>`
- 适配器降级（守信条六「降级需显式」）：`[Compat][Ore] Ore adapter degraded - both classes unresolved: ...`
- 配置同步拒绝（限频汇总，非逐包成功日志）：`[ChainConfigSync] Client dispatcher rejected N packet(s); dropped without cross-lifecycle retry`
- 客户端 UILib 集成完成：`[ClientInit] stage=uilib-integrations-ready`
- Fortune Mixin 能力门：`[Compat][Fortune] mixin=... target=... apply=... reason=...`
- 自动工具客户端静默决策：`[AutoToolSwapClientDiag] reason=cycle-disabled|cycle-ineligible|held-usable|no-candidate|takeover-disabled|takeover-inventory-unsafe|takeover-no-candidate|takeover-request-ignored`
- 自动工具松键事实采样失败：`[AutoToolSwap] release fact capture failed; preserving round for retry`（保留旧 round/ledger，等待后续有效 tick 恢复）
- 自动工具服务端动作结算：`[AutoToolSwapDiag] action ... actionSeq=<关联值> action=<TAKEOVER|DECLINE_TAKEOVER> ... outcome=<...> reason=<...>`；v4 中接替动作的 `actionSeq` 日志字段实际承载独立 `takeoverRequestId`，普通动作仍承载 `actionSequence`。TAKEOVER 固定 reason 包含 `takeover-gate|round-state|inventory-context|selected-slot|pending-anchor-changed|candidate-fingerprint|candidate-low-reserve|ledger-old-role|ledger-active-role|slot-conflict|inventory-read-failed|applied|sync-failed`，非空 anchor 的精确无候选 DECLINE 固定为 `no-candidate`，其他动作稳定为 `none`。每个已进入结算的动作最多一条，不含 NBT 或完整 fingerprint。
- 自动工具接替等待门终态：`[AutoToolSwapDiag] takeover-gate ... state=<SKIP_TARGET|STOP> cause=<...>`；请求发送、deadline、目标/执行身份漂移等 request-local 原因收口为 `SKIP_TARGET`，key release、phase close、round-state 与 external-stop 等硬收口保持 `STOP`。每个 pending gate 仅在首次离开 `WAITING` 时输出一次；committed publication pending 不输出伪终态而继续 WAIT，同门若已有终态日志，迟到 action 的 `takeover-gate` reason 会被抑制，二者应当二选一而不重复。
- 连锁执行结束汇总：`[ChainPlanDiag] player=<UUID> round=<id> generation=<gen> stage=ExecutionFinished reason=<reason> confirmed=<N> consumed=<N> skipped=<N> succeeded=<N>`。目标局部拒绝不逐目标刷日志；`consumed>0 && succeeded=0` 仍会发布 `ExecutionAdvanced(executedThisTick=0, remainingTargets=...)` 喂 watchdog。

### `runServer25` 服务端日志

主目录：`run/server/logs/`（与 client 对称；**当前未跑过 runServer25，目录首次跑会自动创建**）

| 文件 | 用途 | 说明 |
|---|---|---|
| `fml-server-latest.log` | **主诊断入口**（同 client 的 `fml-client-latest.log`，含所有 `[qz_miner/]` 日志） | 服务端独有玩家登录/切维度的服务端侧诊断 |
| `latest.log` | minecraft 主日志子集 | 同 client |

诊断特征串与 client 一致；服务端独有的如 `[PlayerManager] Player logged in` 在服务端更早出现。

### Native 崩溃日志

若 JVM 崩溃（少见，但 mixin/ASM 错误可能触发）会在项目根或 `run/client/`/`run/server/` 生成 `hs_err_pid*.log`。这种情况派 `@explorer` 或 `@oracle` 优先看该文件 stacktrace。

## Issue 症状路由（维护者内部）

> 玩家提交 Issue 时只需描述遇到的问题并上传手边对应的最新日志，不必筛选时段、marker 或分析内容。下表是维护者内部的排查入口，不是玩家提交清单：维护者负责从日志中筛选与症状直接相关的时段和 marker；上下文仍不足时，再围绕具体 Issue 补最小埋点或提出单点追问，不要求玩家执行测试或填写环境矩阵。

| 玩家描述的症状 | 优先日志 | 优先检索的 marker / 信息 |
|---|---|---|
| 启动失败、界面初始化失败、HUD 不显示或渲染异常 | 对应侧 `fml-*-latest.log`；JVM 崩溃时再看 `hs_err_pid*.log` | FML stacktrace、`[ClientInit]`、Qz-UILib 初始化异常、`[ChainPreview]` |
| 连锁不启动、中断、范围或预览异常 | 对应侧 `fml-*-latest.log` | `[ChainStateMachine]`、`[ChainPlanDiag]`、`[ChainWatchdog]`、`[ChainPreview]`、`[ParallelTick]` |
| 掉落数量、聚合或释放异常 | 服务端或单人客户端 `fml-*-latest.log` | `[ChainDropCollector]`、对应时段的 `[ChainStateMachine]` / `[ChainWatchdog]` |
| 自动工具未接替、错误换位或未恢复 | 对应侧 `fml-*-latest.log` | `[AutoToolSwapDiag] action` 的 `outcome/reason`、`[AutoToolSwapClientDiag]`、`[AutoToolSwap] release fact capture failed`；若有扩展 ID 越界异常，核对 `blockId/metadata/max/metadataMax`，若旧 round 长期停在 `CLOSING ledger=true`，沿同一 round 查 RESTORE/CLOSE |
| 配置未生效、不同步或方块 Picker 异常 | 对应侧 `fml-*-latest.log` | `[ChainConfig]`、`[ChainConfigSync]`、`Block picker degraded`、相关 stacktrace |
| GT 线缆替换、连接刷新或矿石时运异常 | 服务端或单人客户端 `fml-*-latest.log`；必要时补看 `GregTech.log` | `[CableCompat]`、`[CableReplace]`、`[Compat][Ore]`、`[Compat][Fortune]` |
| 多人连接、客户端与服务端状态不同步 | 客户端与服务端 `fml-*-latest.log` 中与症状直接相关的一侧或时段 | `[Network]`、`[ChainConfigSync]`、对应时段的状态机 marker |
| 登录、退出、重生、切维度或退主菜单后状态残留 | 对应侧 `fml-*-latest.log` | `[Lifecycle]`、`[PlayerManager]`、`[ChainStateMachine]`、`[ParallelTick]` |

日志取用遵循隐私最小化：维护者从报告者上传的对应日志中筛选相关时段或 marker；上下文确实不足时，才围绕该 Issue 做最小诊断补强或定向请求一个具体文件，不把分析与测试工作转交报告者，也不默认索取客户端与服务端双侧材料。

## 后续补充项

- 项目专属调试命令。
- 一键脚本命令。
- 验收流程命令。

## 维护规则

- 只有实际可执行、可复用的命令才能写入。
- 命令或环境前提变化后及时更新本文件。
