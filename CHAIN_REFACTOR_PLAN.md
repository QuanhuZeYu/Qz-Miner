# 连锁系统重构规划

## 目标

重构现有连锁功能，降低客户端、服务端、网络、预览、执行、搜索之间的耦合度。

重构后的系统需要满足：

- 状态来源单一，避免客户端与服务端状态漂移
- 搜索、执行、预览、网络各自独立
- 长耗时搜索可以在并行 Tick 框架中跨 Tick 分片执行
- 世界修改只能由主线程执行
- 玩家离线、切维度、重生、单人退出时可以正确清理连锁状态

## 当前现状

已落地：

- 状态层：`ChainPlayerState`、`ChainClientState`、`ChainSession`、`ChainStateService`
- 模式层：`ChainMode`、`ChainModeRegistry`、`ChainModeDefinition`、`ChainModeBootstrap`
- 规划层骨架：`ChainPlanner`、`ChainPlanningStrategy`、`ChainTraverser`、`ChainBlockMatcher`、`ChainSearchContextFactory`
- 执行层骨架：`ChainExecutor`、`ChainActionExecutor`、`BlockHarvestActionExecutor`、`ChainDropCollector`
- 客户端预览：`ChainPreviewController`、`ChainPreviewState`
- 网络同步：`PacketKeyState`、`PacketChainModeSwitch`、`PacketChainStateSync`

当前闭环：

- `CHAIN` 模式已经具备输入、规划、执行、HUD、预览的完整主链路
- `AREA` 模式已经具备盒扫规划、执行、预览与 HUD 同步的默认闭环
- `INTERACT` 模式已经具备服务端右键触发、邻居洪泛规划、默认同类交互与作物交互子模式闭环
- 主模式与子模式已经具备统一状态、切换、同步与会话快照框架
- 并行线程负责搜索，主线程负责实际破坏方块
- 生命周期清理已经接入玩家登录、重生、切维度、退出流程

当前限制：

- 实际业务仍主要围绕方块目标，实体目标仍未进入主线
- `SPECIAL` 仍未完成实际策略装配
- 抽象层已存在，但默认实现仍偏向单一路径

## 核心原则

- 状态统一：客户端只维护输入态和显示态，服务端维护权威执行态
- 规划与执行分离：规划只做搜索和筛选，不直接改世界
- 预览与执行分离：可以共享规则，但不能共享生命周期控制器
- 并行只做计算：世界写入、掉落生成、真实破坏必须留在主线程
- Mixin 只做边界补钩子：不承载连锁业务决策
- 模式只做装配：`ChainMode` 只做标识，业务能力由策略对象承担

## 里程碑

### 里程碑 A：基础闭环稳定

状态：已完成

范围：

- 状态层、模式层、客户端预览、服务端执行、基础网络同步全部接通
- `CHAIN` 模式可跑通最小可用闭环

验收标准：

- 连锁键、模式、执行状态有统一存放位置
- 客户端显示状态能跟随服务端同步
- 并行线程不直接修改世界

### 里程碑 B：旧模式迁移到新架构

状态：进行中

目标：

- 将旧版复杂模式逐步迁入新策略层
- 不再回退到旧的高耦合 `Manager / Founder / Operator` 结构

待办：

- [x] 补齐 `AREA` 模式的规划策略与执行装配
- [x] 补齐 `INTERACT` 模式的规划策略与执行装配
- [x] 迁移作物模式
- [ ] 迁移原木/矿脉模式
- [ ] 整理特殊模式与必要兼容逻辑

验收标准：

- 新模式接入后不破坏现有 `CHAIN` 主链路
- 旧规则迁移后统一归入 `chain` 模块内的策略层

### 里程碑 C：策略层彻底解耦

状态：进行中

目标：

- 将当前“抽象已建立、默认实现仍偏单路径”的结构继续拆开
- 让规划器和执行器的新增成本降到只补策略和注册

已完成前置：

- [x] 引入 `ChainSession`
- [x] 引入 `ChainPlanningStrategy`
- [x] 引入 `ChainActionExecutor`
- [x] 将 `ChainModeRegistry` 升级为模式定义注册表
- [x] 将默认规划实现拆出 `Traverser / Matcher / SeedResolver / ContextFactory` 的初步骨架
- [x] 为 `AREA` 接入壳层盒扫遍历器，并与 `CHAIN` 统一共享可挖掘判定规则
- [x] 将客户端预览切换为按模式复用 `CHAIN / AREA` 遍历逻辑
- [x] 将 HUD 调整为同时显示服务端真实匹配数、服务端区域信息与客户端预览匹配数
- [x] 引入统一 `ChainSubMode` 框架，支持主模式下挂载多个子模式
- [x] 将子模式切换、同步、会话快照统一为通用链路，而非 `AREA` 专用字段
- [x] 将 `CHAIN / AREA / INTERACT / SPECIAL` 的默认子模式装配收拢到 `ChainModeDefinition`
- [x] 将预览层与 `AREA` 匹配规则选择下沉到模式定义层
- [x] 抽取 `AbstractFloodFillPlanningStrategy`，收敛 `CHAIN / INTERACT` 的洪泛规划公共主流程
- [x] 将 `INTERACT_CROP` 的候选遍历规则下沉到 `ChainSearchContext`

待办：

- [ ] 继续压缩默认实现中的模式特化判断
- [x] 把更多搜索规则下沉到 `Traverser / Matcher / Context` 组合层
- [ ] 让调度器只依赖抽象接口，不依赖“挖方块”这一种默认能力

验收标准：

- 新增一种规划器变体时，不需要改核心主流程
- 新增一种执行器变体时，不需要改核心主流程
- 主流程只负责调度，不负责承载具体业务细节

### 里程碑 D：统一候选目标与多目标执行链

状态：暂缓

定位：

- 这是未来扩展项，不是当前主线目标
- 实体目标只是未来可选方向，当前只做前置布置

当前已有准备：

- `ChainTarget` 已具备方块列表、实体 ID 列表与存储模式字段

重启条件：

- 明确要接入实体规划、实体交互、实体攻击或混合目标执行

后续待办：

- [ ] 引入 `ChainCandidate` 或等价统一目标抽象
- [ ] 让执行器显式处理 `BLOCK / ENTITY / ALL`
- [ ] 评估客户端预览链对统一目标模型的适配

### 里程碑 E：模式注册与装配完善

状态：已完成

目标：

- 把模式注册流程收拢为清晰、集中、可校验的装配入口

待办：

- [x] 为 `AREA`、`INTERACT` 等模式提供明确占位定义或空实现策略
- [x] 为主模式补齐子模式注册、默认值与切换逻辑
- [x] 继续清理 `ChainModeBootstrap` 的装配职责
- [x] 为模式定义注册补充初始化入口和校验逻辑
- [x] 补充缺失定义、冲突注册的日志与保护

验收标准：

- 模式注册过程清晰、集中、可审查
- 新增模式时只需新增策略并在装配入口注册
- 缺失定义时系统具备明确日志和退化行为

## 当前主线顺序

后续开发按以下顺序推进：

1. 先完成旧模式向新策略层的迁移
2. 再继续拆分默认规划实现中的规则与遍历职责
3. 再完善模式注册与装配校验
4. 最后视实际需求决定是否启动统一目标抽象

### 里程碑 F：恢复连锁目标预览渲染

状态：待开始

目标：

- 将“连锁目标预览渲染”重新接回当前客户端链路
- 保持当前 `ChainPreviewController -> ChainPreviewState -> 渲染层` 的解耦结构
- 明确旧预览代码只迁移“渲染与网格生成能力”，不回退旧的搜索、输入、生命周期控制逻辑

实现边界：

- 保留现有 `ChainPreviewController` 作为唯一预览搜索与状态驱动入口
- 保留现有 `ChainPreviewState` 作为预览结果快照来源
- 新增纯渲染层，仅读取预览状态并绘制，不创建搜索任务、不接管输入态
- 旧代码中的 `BaseChainViewer` 不整体迁回，只拆分复用其中可独立存在的渲染数据处理部分

旧代码适配原则：

- `MinerRenderer` 的职责迁为新的 `ChainPreviewRenderer`
- `BaseChainViewer` 的搜索、任务注册、生命周期控制逻辑废弃，不进入新架构
- `SpaceCalculator` 可迁为新的预览网格构建器，输入改为 `ChainTarget` 列表
- `RenderCache` 可迁为新的预览网格缓存层，只负责 GPU 缓冲与绘制
- 旧 shader 不是首要目标，优先恢复基础线框预览，再评估是否接回 shader 效果

待办：

- [x] 在文档中固定职责边界，避免把旧搜索逻辑重新塞回渲染层
- [ ] 为 `ChainPreviewState` 补充渲染层可用的版本标记或等价脏标记
- [ ] 新增 `ChainPreviewRenderer`，在客户端渲染事件中消费预览快照并绘制
- [ ] 从旧 `SpaceCalculator`/`RenderCache` 中提取可复用的网格构建与缓存逻辑
- [ ] 先跑通基础线框预览，再决定是否接入旧 shader

验收标准：

- 按住连锁键并瞄准目标时，客户端能稳定显示当前预览目标集合
- 切换目标、切换模式、松开按键时，预览渲染能正确刷新和清理
- 渲染层不直接依赖旧 `Founder / Viewer / Manager` 结构
- 预览渲染不破坏现有 HUD、客户端预览计算与服务端执行链路
- 首版允许仅实现基础线框效果，不要求一步到位恢复旧 shader 表现

## 暂不做

- 一次性迁移所有旧模式
- 立即恢复所有旧兼容逻辑
- 让客户端预览和服务端执行共用控制器
- 让规划线程直接改世界
- 在 Mixin 中承载业务逻辑
- 一开始就引入过重的泛型体系
- 让 `ChainMode` 枚举直接承担业务逻辑
- 为了实体目标提前打断当前方块连锁主链路的收敛

## 结论

当前重构已经完成基础闭环，后续重点不再是继续铺新骨架，而是：

- 把旧模式逐步迁到新策略层
- 继续做规划层和执行层的职责收缩
- 完善模式装配入口
- 将实体目标保留为未来可选扩展，而不是当前主线任务

补充说明：

- `INTERACT` 当前已具备默认右键交互闭环，支持真实点击面、命中点与 `INTERACT_CROP` 子模式
- `CHAIN / INTERACT` 的洪泛规划公共主流程已经完成一次收缩，但 `AREA` 与执行调度层仍有进一步抽象空间
