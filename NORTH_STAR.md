# Qz-Miner 设计导向标

## 中心思想

> 并行线程快速、可中断地计算要处理的方块；服务端主线程独占真实世界写入。两者以有界工作和安全屏障协作，玩家掉落不因任务或生命周期切换丢失。

本文件只保留跨模块、出错后果明确的长期原则。局部实现细节以源码、测试、`docs/设定值层/边界.md`、错误记录和专题决策为准。

## 原则

### 世界主权

- 方块破坏、掉落实体生成和世界状态修改只在服务端主线程。
- 并行 worker 只读世界并发布候选或完成事实，不直接切换主线程执行状态。

Minecraft 1.7.10 世界不是线程安全的。违反此边界会造成竞态、状态损坏或 native crash。

### 有界并行

- 大范围规划必须能按预算让出执行时间并从安全点恢复。
- 停止 worker 使用协作式取消；`ParallelTickExecutor.endStage(...)` 必须等 active worker 到达安全边界。
- 常规流程不得用超时强杀或 `Future.cancel(true)` 代替收口；最终执行器 shutdown 兜底除外。

### 玩家资产

- 已收集掉落与单次会话生命周期解耦。
- 掉落实体生成失败或异常时保留或回填，生命周期清理不得吞掉已收集物品。

### 线程与生命周期

- 网络入包和客户端状态同步在对应主线程处理后再修改游戏语义状态。
- 玩家退出、重生、切维度、克隆和单人退主菜单统一清理玩家状态。
- 客户端断线或世界卸载时，在客户端主线程停止预览任务并释放 GPU 资源。

### 兼容降级

- 可选模组缺失、链接失败或签名不兼容是正常情况，能力不可用时安全降级。
- 类探测避免静态初始化；反射成员解析处理 `LinkageError` 和 `SecurityException`；dedicated server 不扫描客户端专属签名。
- GTNH 上游升级后，复核依赖上游实现形状的适配点，例如矿石时运截断和线缆刷新。

## 数据流

```text
触发 -> 并行预算化规划 -> 主线程消费候选并修改世界 -> 玩家级掉落缓冲 -> 统一生命周期清理
```

规划完成只表示不再产生新候选。是否进入执行终态由主线程结合候选消费与完成事实决定。

## 变更原则

- 修改上述原则、状态机、线协议、持久数据或公共兼容承诺前，说明实际后果并取得用户确认。
- 已知且有意的例外记录在下方；没有明确后果的局部取舍不需要升级为宪章条款。
- 已解决的例外直接移除，长期技术细节回写对应决策或错误文档。

## 已知例外

### GT 线缆替换单 tick 原子执行

`ChainMode.SPECIAL` 的 GT 线缆替换在规划完成和资源/规模预校验通过后，于单 tick 内执行完整链路。它有意牺牲通用分片控速，以避免新旧电压线缆跨 tick 共存导致机器爆炸或线缆烧毁。范围仅限该特殊模式；详见 `docs/反馈层/决策/gt-cable-replacement-model.md`。
