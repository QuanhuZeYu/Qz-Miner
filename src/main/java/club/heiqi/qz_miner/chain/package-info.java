/**
 * 连锁框架指导 —— 本包及其子包共享的「权限与真源划分」原则。
 *
 * <p>这一页是<b>指导</b>，不是明细清单：它只说明"谁能改什么"这一层。各实现类在 javadoc 的
 * {@code <h3>守框架约束</h3>} 小节里逐条声明自己遵守了哪几条，那里写的是<b>该类的具体行为</b>
 * （随代码演进），因此留在类注释而不搬到这里——集中收录逐类行为必然与代码分叉。</p>
 *
 * <h3>原则</h3>
 * <ol>
 *   <li><b>唯一写权威</b>：{@code phase}/{@code generation} 只由 {@link club.heiqi.qz_miner.chain.statemachine.ChainStateMachine}
 *       写；其余组件<b>只 publish 不 transition</b>，客户端投影与状态机物理隔离（只可见、不可切态）。
 *       <br>派生自偏好：框架正确（单一真源）。各组件各自改状态即缝补式补丁，禁止。</li>
 *   <li><b>边界不越权</b>：每个组件只碰自己的容器与自己的副作用面——不切 phase、不碰 worker、
 *       不写世界、不调破坏 API。事件对象以"构造后不可变"体现同一条约束。
 *       <br>派生自偏好：框架正确（职责边界即约束）。</li>
 *   <li><b>协作式取消</b>：超时/中止只 publish 事件，不 {@code Future.cancel}、不强杀 worker、
 *       不绕收尾路径；worker 自行停在安全边界。
 *       <br>派生自偏好：拒绝缝补式补丁（不靠暴力兜底掩盖状态不一致）。</li>
 *   <li><b>跨线程只经事件总线</b>：worker 只入队（{@code publish}），主线程 drain 消费；
 *       跨线程共享只经 {@link java.util.concurrent.ConcurrentHashMap} 等显式可见性手段。
 *       <br>派生自偏好：框架正确（可推理的并发模型优先于局部加锁）。</li>
 *   <li><b>生命周期收口</b>：失败计数、镜像、滞回记忆、玩家槽位随生命周期事件一并清零或移除，
 *       不得跨会话累积；只增不减的容器必须能释放。
 *       <br>派生自偏好：有界、可失效、可释放（愿用内存换性能，但必须有界）。</li>
 *   <li><b>复用既有入口</b>：不新造遍历、工厂或生命周期源；复用现成 SPI 与共享实现。
 *       <br>派生自偏好：优先复用目标仓既有能力。</li>
 *   <li><b>显式收尾</b>：终态路径显式走完（{@code FINISHING} 态与清理事件），不留半途状态靠超时兜底。
 *       <br>派生自偏好：框架正确（失败语义显式）。</li>
 *   <li><b>输入语义分层</b>：持续意图用「电平」，动作意图用「边沿」。{@code ChainKeyPressed} 携带的是
 *       按键电平（按住 = 授权持续有效，收尾后按电平自动再武装 T11），观测事件才是边沿（一次点火一轮）。
 *       不得把持续意图建模成一次性边沿——"按住期间不会再有第二次边沿"是常态，不是越界；
 *       否则按键会表现为"只能连锁一次"，且只能靠客户端补发兜底。
 *       <br>派生自偏好：框架正确（语义与玩家意图一致，不靠缝补式补发）。</li>
 * </ol>
 *
 * <h3>优先级与演进</h3>
 * <p>判断顺序：<b>用户工程偏好 &gt; 本指导 &gt; 类级约束</b>。本页是下级——偏好变动时按偏好改，
 * 改完回查引用本页的类注释是否需要同步。偏好的真源与派生关系登记在工作站
 * {@code 用户偏好-工程取向.md}。</p>
 */
package club.heiqi.qz_miner.chain;
