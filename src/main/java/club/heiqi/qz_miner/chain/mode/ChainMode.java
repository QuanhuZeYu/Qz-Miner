package club.heiqi.qz_miner.chain.mode;

/**
 * 连锁模式定义。
 */
public enum ChainMode {
    /**
     * 默认同类连锁模式。
     */
    CHAIN,
    /**
     * 范围模式。
     */
    AREA,
    /**
     * 交互模式。
     */
    INTERACT,
    /**
     * 预留的特殊模式。
     */
    SPECIAL
}
